<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version (16.2.6) has breaking changes — APIs, conventions, and file structure may differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing routing/config code. Heed deprecation notices. Notably: middleware lives in `proxy.ts` (exports `proxy`, not `middleware`); `params`/`searchParams` are Promises you must `await`. There is no `middleware.ts`.
<!-- END:nextjs-agent-rules -->


# Maintainer note

The session-by-session build journal lives in `private/SESSION-LOG.md` (untracked; the `private/` folder holds all maintainer-only material). Read it at session start; append a summary of each working session there, not here. The living backlog is `private/docs/TODO.md`; its top item is the immediate next step (as of 2026-08-19: an ultra code review of this tree, run by the maintainer; the session after it triages the findings).

**Cheap-execution mode** (2026-08-30): Claude Code has no `fableplan` alias (verified against the 2.1.251 binary; the built-in splits are `opusplan` = Opus plans / Sonnet executes, and `haiku` = Sonnet plans / Haiku executes). The maintainer's setup instead keeps Fable as the main model and delegates through the untracked `.claude/agents/executor.md` subagent (pinned `model: sonnet`, locally git-excluded). During bulk task runs: plan and review in the main session, hand each well-specified implementation task to `executor`; keep gnarly debugging in the main session. Re-check for a native Fable plan split on CLI upgrades.

**Operational rule** (2026-09-02): never smoke-test `/api/cron/*` between 00:00 and 09:00 UTC (20:00-05:00 ET). The day-keyed engines (scan, intel, research) treat any hit in that window as the new day's run and the pipeline's 06:00 UTC boundary exists specifically because one such smoke test consumed the next morning's run key. Use each console's manual run instead (the pipeline's is cadence `manual`).

---

# The AI Atlas

## What this is
A **single-user tool for staying oriented in the AI-economy debate**, with **two public surfaces over one body of material.** The **Argument Map** (`/map`) maps *where the disagreement actually is*: open questions, candidate stances, the falsifiable claims they stand on, the bridge-claims that link domains, and the evidence. The **Signal Board** (`/signals`) is a feed of tracked AI developments, sorted by audience lens and wired back to the claims they touch, fed by an admin **discovery pipeline** (`/pipeline`). A private **personal layer** (confidence, rationales, source priors) sits on top of a public **share view**. The goal is **orientation, not proof** — the model proposes, the human commits. Design docs live in `docs/`.

## Stack & how to run
- **Next.js 16.2.6** (App Router, Turbopack — enabled by default in dev; the workspace root is pinned in `next.config.ts` via `turbopack.root`) · **React 19.2.4** · **TypeScript** (strict) · **Tailwind v4** (CSS-first, `@import "tailwindcss"` + `@theme` in `app/globals.css`; no `tailwind.config`) · **Supabase Postgres** (accessed via the `pg` pool at runtime; the Supabase JS client is not a dependency — all reads go through `pg`).
- **3D**: `three@0.180` powers `/traceroute` only. Scenes are described in data and built from primitives (no model assets, no loader), all scene mutation lives in module-scope functions under `lib/traceroute/scene-*.ts`, and the canvas is `next/dynamic({ ssr: false })` behind a WebGL probe with an SVG fallback. See `docs/traceroute.md`.
- **AI layer**: `@anthropic-ai/sdk` powers the Claude calls across `lib/dossier.ts` (the shared `runStructured`), `lib/summary.ts` (question-state summaries), `lib/lens.ts` (lens recommendations), and `lib/pipeline/*` (the discovery pipeline — `web.ts` does web-grounded discovery **plus the candidate fetch layer** (`fetchCandidateText`: direct HTML/PDF fetch with `unpdf` extraction, NUL/control-char sanitization, terminal-vs-transient `FetchFailure` classification, and an `r.jina.ai` reader fallback for bot-walled/paywalled hosts — optional `JINA_API_KEY` lifts its keyless rate limit), `analysis.ts` is the **single signal proposer** that drafts signals for both the pipeline and the manual *Turn into signal* flow, `triage.ts` filters + URL-dedups). `unpdf` also does browser-side PDF→text extraction on the add-source form (text only — the file is never stored). See the AI integration bullet in *Architecture & conventions*; the fetch-layer diagnosis/fix history was `docs/pipeline-fetch-diagnosis.md`, since deleted.
- **Console design system**: the visual language lives in `app/styles/*.css`, imported **in cascade order** from `app/globals.css` (which then re-bases the `@theme` color tokens onto the design vars and re-asserts the plotting-grid body background): `tokens` → `base` → `components` (shared controls) → one sheet per surface (`signal-board`, `sitenav`, `concepts`, `supply-chain`, `ask`, `lobby`, `rail`, `showcase`, `feedback`, `paper-reader`) → `home` (the `.bs` broadsheet layer) → `traceroute` (scoped to `.tr`) → `costs-deck` (the `.cdk-*` 16:9 deck stage) → `education` (scoped to `.edu`) → `tooling` (`.tl-*`) → `reports` (`.rp-*`). The import ORDER is the cascade; a new surface sheet registers in `globals.css` at its cascade position. The `design-system/` folder once referenced was retired in `2feaccf`; the stylesheets are the contract.
- Setup:
  1. `npm install`
  2. Copy `.env.example` → `.env.local` and fill it in.
  3. `npm run db:migrate` — apply `supabase/migrations/*.sql`.
  4. `npm run db:seed` — load questions/stances/claims/bridges/frame (all confidences reset to **0.50**, the author's to move).
  5. `npm run dev` → http://localhost:3000
- Scripts (`package.json`): `dev`, `build`, `start`, `lint`, plus `db:migrate` (`scripts/migrate.mjs`, tracks applied files in a `_migrations` table), `db:seed` (`scripts/seed.mjs`, idempotent upserts on `code`/`slug`), `db:seed:tooling` (`scripts/seed-tooling.mjs`, overlays the untracked `private/tooling-categories.json` on the 0054 starter categories, idempotent on `slug`), `db:seed:concepts` (`scripts/seed-concepts.mjs`, idempotent on `slug`, never clobbers prose — kept separate from `db:seed` because THAT seed resets every confidence to 0.50), `db:verify` (`scripts/verify.mjs`, sanity-checks the seed). `scripts/test-loop.mjs` (run directly with `node`, not an npm script) is a **rollback-only** smoke test of the evidence→confidence→rationale→snapshot loop. All `.mjs` scripts load `.env.local` via dotenv and use the **direct** `SUPABASE_DB_*` connection.

## Architecture & conventions
- **All DB access is server-side.** `lib/db.ts` owns a global `pg.Pool` connecting as a role that **bypasses RLS**. The *server* decides public (the map) vs. personal (confidence, rationales, priors). `lib/db.ts` throws if imported in the browser. Helpers: `q()`, `one()`, `exec()`, `withTx()`. Never import `lib/db` into a client component.
- **numeric is parsed as a JS number.** `lib/db.ts` calls `pg.types.setTypeParser(1700, (v) => parseFloat(v))` so confidences (Postgres `numeric`, OID 1700) arrive as numbers, not strings — they're used in sliders / `toFixed`. (Note: `date`/`timestamptz` still come back as JS `Date` objects — cast to text in SQL, e.g. `to_char(...)`, when a string is needed.)
- **Admin/Guest gate** (`lib/auth.ts`): admin session is a **signed HMAC cookie** (`atlas_admin`, value `admin.<hmac>`); guest is a non-privileged UX flag (`atlas_guest`). `secret()` **fails closed** — throws if `AUTH_SECRET` is missing or `< 32` chars (no public fallback that could be forged). `isAdmin()` is the real security boundary (verifies the signature with `timingSafeEqual`); `checkPassword()` compares fixed-length HMAC digests via `timingSafeEqual` and returns false if `ADMIN_PASSWORD` is unset. `isPreview()`/`isEditMode()` are unsigned admin-only UX flags; `shareToken()`/`verifyShareToken()` back the public `/share` link. **`proxy.ts` is open by default (2026-09-23)**: every page renders sessionless now, so the proxy no longer carries a page allow-list. It fences exactly one thing: `/api/*` routes with no in-route session check of their own (everything outside `/api/` is public, plus a short API allow-list: `/api/traceroute/tokenize`, `/api/datasets/*`, `/api/portal/ask`, `/api/ask/peek`, `/api/ask/doc`, `/api/tooling/events`, `/api/nav/viewer`, `/api/tickets`, `/api/cron/*`); an unlisted `/api/*` route without the `atlas_admin`/`atlas_guest` cookie still bounces to `/login`. It also rewrites **malformed detail URLs** (a non-UUID id under `/signals`, `/research`, `/scout`, `/source`, `/theses`, `/thesis-report`, `/reports`, `/reports/sheet`, or an impossible `/blotter/<day>`) to an unmatched path for a real 404 (`isMalformedDetailPath` in `lib/route-shapes.ts`, tested): every page streams behind the root `loading.tsx`, which locks the status at 200 once it paints, so an in-page `notFound()` for a well-formed but missing id still renders the not-found UI with `noindex` and a 200 (Next's documented behavior; an existence check in the proxy would add a query to every detail click). A new `[id]` route's static siblings must be added to `UUID_ROUTES` there. Admin-only PAGES gate themselves instead, with `adminGate(pathname, title)` (`lib/admin-gate.tsx`, kept out of `lib/auth.ts` because it returns a component and `lib/auth.ts` stays JSX-free): `const gate = await adminGate('/worldview', 'Worldview & spine'); if (gate) return gate;` renders `<AdminOnly>` (`components/AdminOnly.tsx`, a notice under the layout's chrome with a link back to the page's nearest public parent, from `publicParentFor()` in `lib/nav.ts`) for a non-admin instead of redirecting. `isAdmin()` remains the real security boundary everywhere (components and server actions re-check it); the proxy and the page gate are both routing convenience on top of it.
- **Personal layer stripped server-side** (`lib/data/*`, barrel-imported as `@/lib/data`; the strippers live in `lib/data/shared.ts`): `strip()` nulls `confidence`/`confidence_label`; `stripClaim()` additionally nulls `domain_note`; `getEvidenceFor()` nulls `reliability_prior`/`note` for guests; `getQuestions(personal)` zeroes `contested_count` and nulls `last_moved`; rationales are returned only for admins. **Guest mode IS the share view** — there is no separate share renderer. On the Signal Board, guests see only **published** signals (drafts are admin-only); a touched claim's confidence word and the model's per-touch **reason** (`signals.touch_details`) are admin-only, while a touch's **direction** is descriptive and shown to all. `resolveTouches` flags unresolved codes for admins (drift guard).
- **Server actions** (`lib/actions/*`, one `'use server'` module per domain plus a directive-less `index.ts` barrel and a `shared.ts` for the guards/parsers — never re-export or client-import `shared.ts`): every action calls `requireAdmin()` first and validates input (allow-listed enums, prior 0–100, confidence 0–1, `UUID_RE`, `safePath()` blocks open redirects). They call into `lib/mutations/*` (the only writer, barrel-imported as `@/lib/mutations`) and `revalidatePath('/', 'layout')`. (Some AI actions are typed-arg and return data to the client rather than redirecting — e.g. the pipeline step actions and the manual signal flow's `prepareSignalFromSourceAction`/`overrideAndApproveAction`.)
- **AI integration:** **most** AI features — source dossier, PDF metadata auto-fill, claim recommendations (recommend-only), lens recommendations, concept prerequisite/claim-wiring recommendations + the concept gap diagnosis (`lib/concepts.ts`, recommend-only), the argument-map gap diagnosis + claim/bridge edge recommendations (`lib/argument-gaps.ts` / `lib/argument-nodes.ts`, recommend-only), question-state summaries, and Signal Board draft proposals — route through `runStructured` in `lib/dossier.ts`: a single forced-tool, **non-web** Anthropic call (`claude-sonnet-4-6`, thinking off, effort low/medium per caller) returning validated JSON. `lib/summary.ts` reuses it (metrics computed in code, the model only narrates). The web-enabled paths are the discovery pipeline, scout discovery (`lib/scout/web.ts`, same call shape), and the /ask web toggle; the pipeline's primary (`lib/pipeline/web.ts` `searchCandidates`): a standard `messages.create` with the GA `web_search` tool (`web_search_20250305`, no beta header) plus a `submit_candidates` client tool. To fit Vercel's hard ~60s cap it is **decomposed into many short, DB-checkpointed units** (one lens's query batch, ≤3 searches ≈ 45s), not one long call; the analysis step is likewise **two invocations per candidate** — `hydrateCandidateAction` (fetch + cache `raw_content`, with the reader fallback) then `analyzeCandidateAction` (the model leg). Discovery passes `blocked_domains` (curated deny-list + learned zero-yield domains — guardrails in `getZeroYieldDomains`: duplicates count as YIELD, a never-auto-block allowlist protects primary sources/wires, and only the trailing 90 days count so blocks can decay) to the search tool, and the run-static prompt halves (analysis target list, triage digest) live in the cached system block. Two more web-enabled units per run (the Kimi-K3-miss fixes): the **breaking-events sweep** (`searchBreakingSweep` — lens-agnostic, significance-first, over a curated `allowed_domains` outlet list; every entry must be crawlable by Anthropic's agent or the API 400s — reuters/apnews/nytimes/wsj/ft/economist/theverge/arstechnica all block it) and the **post-run coverage check** (`lib/pipeline/coverage.ts` — re-derives the window's biggest AI developments with independent phrasing, marks each covered/missed vs. the run's candidates + existing signals, persists to `pipeline_runs.coverage`, advisory only). Lens query templates resolve `{year}` AND `{month}` from the run window (`resolveDateTokens`) — month-anchored, news-shaped phrasing, because evergreen year-only queries surfaced SEO listicles instead of the week's news. Triage carries a **materiality override** (a major uncovered development gets approved even from a weak source). Requires server-only `ANTHROPIC_API_KEY`.
- **The human gate** (`moveConfidence` in `lib/mutations/core.ts`): transactional via `withTx` — it reads the old confidence, updates it, inserts a `rationales` row (a **rationale is required**; `actions.ts` rejects empty, and the move may optionally cite an `evidence_id` surfaced in `ConfidenceEditor`), and auto-writes a `post_commit` **snapshot** of all confidences (claims, stances, bridge_claims, and `positions_crosscutting`) — all atomic. A confidence can never move without its "why". Both the discovery pipeline and the manual *Turn into signal* flow mirror this gate: each only ever creates a **draft** signal (through the shared `analyzeCandidate` → `createDraftForCandidate`); a human publishes. **Publishing a signal is itself a gate**: `setSignalPublished`/`createSignal`/`updateSignal` call `syncSignalEvidence` (transactional) which materializes one **evidence** row per touched claim/bridge (carrying `signal_id`, `lens`, `direction`, and the reason as excerpt) — so a signal's findings enter the Argument Map only on publish, and unpublish/delete removes them.
- **Confidence: stored as `numeric(3,2)` 0–1, displayed as a word.** A generated column `confidence_label` (Postgres `conf_label()`) maps it: `<0.40 thin`, `<0.60 contested`, `<0.80 leaning`, else `settled`. `components/ConfidenceEditor.tsx` mirrors these thresholds client-side for the live preview. Words/colors are rendered via `lib/format.ts`.
- **Dynamic rendering:** every page that reads cookies/DB sets `export const dynamic = 'force-dynamic'`. Pattern in public pages: `const admin = await isAdmin(); const preview = await isPreview(); const personal = admin && !preview; const data = await getX(slug, personal);`. Admin-only pages (`/ingest`, `/source/[id]`, `/sources`, `/q/[slug]/summary`, `/pipeline`, `/worldview`, `/data`, `/calibration`, `/costs`, `/reports/period`, `/tickets`, `/signals/new`, `/signals/[id]/edit`, `/signals/drafts`, `/scan`, `/intel`, `/tooling/console`, `/agent`, `/research/console`, `/scout/console`, and 9 more, 29 in all) instead gate with `const gate = await adminGate(pathname, title); if (gate) return gate; const admin = true as const;` (`lib/admin-gate.tsx`; renders `<AdminOnly>` for a non-admin instead of redirecting) as their FIRST work — never an inline `isAdmin()` + `redirect` pair. The four Header-less pages (`/costs/deck`, `/ingestion/deck`, `/research/digest`, `/signals/digest`) have no chrome to render a notice into, so they keep a plain `if (!(await isAdmin())) redirect(publicParentFor(pathname).href);`. `requireAdminPage()` (`lib/auth.ts`) still exists for a caller that can't render a component, but no page or route uses it today (route handlers check `isAdmin()` inline). `/signals`, `/signals/[id]`, and `/reports/[id]` are **public**. Routes that host an AI server action set `export const maxDuration = 60` (`source/[id]`, `ingest`, `q/[slug]`, `q/[slug]/summary`, `pipeline`, `reports/period`, `research/*`, `scan`; `tooling/[slug]` and `tooling/reports` declare 120 for the 90s Sonnet deep dive / report legs) — page-level `maxDuration` covers its server actions. The `/api/cron/*` routes (23 `vercel.json` entries) gate on `CRON_SECRET`, not the admin cookie: each opens with `const denied = cronGate(req); if (denied) return denied;` from `lib/cron/shared.ts` (Bearer header, fails closed when the env var is unset; `pingDeadman` for the optional `HC_PING_URL_*` dead-man ping lives beside it, tested by `scripts/test-cron-gate.mjs`); the engine routes declare `maxDuration = 800`, the agent/brief/edition/feeds/roundup routes 300.
- Path alias `@/*` → repo root.

## Routes (`app/`)
- `page.tsx` — **the Lobby** (`force-dynamic`, no `maxDuration`): generic greeting + sub-line, the `LobbyAsk` chat launcher (client; routes `?q=` into `/ask`; blinking accent caret + the `ExamplePill` typewriter under it), then the **widget board** (2026-08-30 rework; mig `0045`): an ordered, admin-customizable set of widgets from `lib/widgets/catalog.ts` (metadata: key/name/desc/`access`/`span`) rendered by `components/lobby/WidgetBoard.tsx` via the key→component map in `components/lobby/widgets/registry.tsx`. One GLOBAL layout in the `home_prefs` singleton (`getHomeWidgets`/`setHomeWidgets`/`saveHomeWidgetsAction`; empty/missing row falls back to `DEFAULT_WIDGETS` ≈ the pre-rework page); guests get the same order minus `access:'admin'` widgets, filtered in `WidgetBoard` BEFORE their data is fetched. V1 catalog (14): the seven portal tiles + the upload door (bare `.lobby-tile` chrome), latest-signals + atlas-stats (public), and the admin quartet: **daily-jobs cron tracker** (`getDailyJobStatus` in `lib/data/readiness.ts`: per-job state over today's scan/pipeline/intel runs — row wins, then paused/off(weekend)/pending — ET finish times, ready headline counts only the jobs that actually ran, firewall dataset download chips when ready), desk-counts, todays-spend (`getSpendWidgetData`; forecast is a 14-day zero-filled mean ×30, NOT `computeForecast` — cycle), tavily-quota. `CustomizeWidgets` (client) = drag/↑↓/add/remove editor. Styles: the `.lw-*`/`.lwc-*` block in `app/styles/lobby.css`. NOTE "readiness" naming: `lib/*/handoff.ts` already means the importer orientation docs, so the tracker's read lives in `lib/data/readiness.ts`.
- `blotter/page.tsx` — **the News Blotter, since 2026-09-23 the Daily Edition**: a public AI newspaper written every weekday from what the engines already stored, never from a new search. `lib/edition/pack.ts buildEditionPack(day)` reads, **since the previous press time** (`windowFor`: 16:45 UTC to 16:45 UTC, Monday back to Friday's), the `scan_items` + `intel_items` (relevance/significance ≥ 0.55, tier ≤ 3, not marketing), approved `signal_candidates`, published `signals` windowed on **`first_published_at`** (mig `0059`: when the signal first went public, stamped once by every publishing writer and never moved; `published_at` is the editorial date, so a draft published days after its article still makes that day's paper), the **engine-analyzed `papers`** (`triage_status = 'kept'` with a non-null `extraction`, never `review_status = 'dismissed'`, human-confirmed tracked/noted first, limit 8; a same-day tracked/noted predicate read 0 every edition because human review lands days after the analyze step; the strip label is "Papers analyzed"), Monday's new tooling entrants, the latest run's coverage misses, and `intel_facts` by company (window = the engines' day, Monday covering the weekend; pipeline rows on the 06:00 UTC day); `lib/edition/cluster.ts` groups items into stories by headline tokens + shared entities and ranks them by relevance × log(coverage) × tier × content kind × an **AI weight** (topic relevance is not AI relevance: items whose text names no AI term score 0.45, lens-sourced items always 1). Two GLM legs (`lib/edition/generate.ts`, features `edition_front` / `edition_column`, `EDITION_DAILY_BUDGET_USD` 0.10): the Axios-style front (4-8 items: headline, why it matters, by the numbers, a go-deeper href that must be in the cluster's allow-list) and a Levine-style column tying the day to claims (`claimsTouched`, filled with the claims that gained the most evidence in 30 days when the board was quiet), citation-gated by `allowlistForEdition` + `enforceCitations` at save and render. Saved as `generated_reports` kind `'edition'` (mig `0057`, the third auto-publishing kind; `edition_prefs` singleton), one per `scope_to` day (enforced by the `0058` partial unique index, so an overlapping cron + manual run reports the second insert as skipped), skipped on quiet days (< 3 clusters). The DB-free helpers (`windowFor`, `allowlistForEdition`, `deterministicFront`, `validateFrontItems`, `thingsHappenFor`) live in `lib/edition/pure.ts` so the plain-Node tests can load them; `pack.ts` is the one DB reader. The pack also fetches two free strips at build time (no cron, no table): the Hacker News front page filtered to AI (`lib/edition/hn.ts`, Algolia) and a market basket (`lib/edition/markets.ts`: NVDA, MSFT, GOOGL, META, AMZN, TSM, SOXX, XLU via Yahoo's keyless chart endpoint, an unofficial feed; the strip is omitted when fewer than 4 symbols answer). A failed model leg falls back to the deterministic front / no column and is recorded in `narrative.dropped`. Pages: `/blotter` (latest, `EditionView`: masthead, numbers strip, market strip with sparklines, front + column, **Things happen full-width in three-up cards**, the Hacker News cards, a prominent Sources block with the outlet count, then Research / Tools / Blind spots), `/blotter/[day]`, `/blotter/archive`, `/blotter/desk` (the old dashboard, admin), and **`/blotter/[day]/pdf`**: a 16:9 deck PDF of the edition (`lib/edition/deck.ts buildEditionDeck` → `renderCostDeckPdf`; every link clickable via the `bullets` slide's new `href`/`meta` fields; the route is in `next.config.ts outputFileTracingIncludes`; "Deck PDF" button in the page top). Cron `/api/cron/edition` weekdays 16:45 UTC (`?day=` backfills; **the scheduled call replaces an edition built before that day's press time**, so a manual morning run is a preview and the 12:45 ET paper still lands, while a named-day backfill or a second manual run never clobbers an existing edition); the 20:30 UTC `/api/cron/feeds` late sweep lands in the next morning's paper. Styles `.ed-*` in `app/styles/edition.css`. Tests `scripts/test-edition.mjs`, `test-edition-pack.mjs`, `test-edition-deck.mjs`.
- `map/page.tsx` — **Claims & Theses** (the lobby's third tile), layered **thesis-first since 2026-08-15** to read as the deal workflow: (1) **Theses**: admin sees the `ThesisStrip` desk (every ACTIVE thesis via `getThesisDesk()`, mapped or not, with gap flags + latest-run splits, each linking `/theses/[id]`; the section head carries the `ThesisDraftBox` type-in: statement + blue Create thesis, which creates the thesis unmapped and lands on its workflow page with the mapping form auto-opened), guests keep the report-anchored `ThesisTracker` (limit 8, links `/thesis-report/[id]` only); (2) **the claims ledger**: `TopClaimsPanel` at limit 10 (evidence counts public, confidence word admin-only) beside the admin `ConfidenceMovementPanel` (`getCalibration().moves`); (3) **The frame**: the six question cards, Bridges/Concepts chips in the section head, and (admin) the map-wide **gap-diagnosis panel** (`ArgumentGapPanel` with prop-injected actions + `diagnoseArgumentGapsAction`, so the page sets `maxDuration = 60`; see *Add-node authoring* below). Both thesis blocks sit in `.bs` wrappers (their styles are `.bs`-scoped in `home.css`). The `home.hero.title`/`home.hero.lede` Editable defaults now read "Claims & Theses" (the content_blocks overrides were deleted 2026-08-15; code is the truth). `ShareNotice` for guests.
- **Theses (the deal workflow)**: `theses/page.tsx` (admin index) · `theses/new` · **`theses/[id]`, the workflow page** (admin, `maxDuration = 60`), a numbered four-step arc: 1 edit the thesis (`ThesisForm`, "✦ Map to Atlas claims" recommend-only mapping), 2 **the logic tree** (`ThesisLogicTree` SVG: thesis → mapped claims/bridges with confidence bands → question chips; gap recs as dashed ghosts linking Start draft; narrow-screen list fallback; data `getThesisTreeData`), 3 **the thesis-scoped gap diagnosis** (migration `0036` `theses.gap_scan`; the shared `ArgumentGapPanel` with `.bind(null, id)` actions; `diagnoseThesisGapsAction` grounds on `buildThesisPack` signals that touch no mapped claim, gate = `validateGapRecommendations` in `lib/gaps-core.ts` with `requireRef: false`; Start draft rides `?gap=<code>&thesis=<id>`, the claim/bridge forms post a hidden `thesis_id`, and the create actions `appendThesisCode` so the rec self-clears), 4 reports (`ThesisConsole`, unchanged). Claim/bridge pages carry **"Theses tracking this claim"** (`getThesesForTarget`; guests link the latest public report only).
- `q/[slug]/page.tsx` — a question: stances, the `QuestionMap` (claims → stances SVG), all claims, the bridge band, and (admin) a "Summarize state" button + an "Add claim" link to `q/[slug]/claim/new`.
- `q/[slug]/summary/page.tsx` — admin: the AI question-state-summary timeline (saved per question, tracked over time). `adminGate` (the `AdminOnly` notice for non-admins).
- `claim/[code]/page.tsx` — claim detail (test, evidence + one-sided warning, "bears on", rationale history). **Frames** (`is_frame=true`) render a distinct view: only what they *organize*, quarantined from evidence.
- `bridge/[code]/page.tsx` — bridge-claim detail (domain↔domain, test, fed-by claims, evidence, rationales).
- `bridges/page.tsx` — overview of all bridge-claims ("the spine"); admin gets an "Add bridge-claim" link to `bridge/new`.
- **Add-node authoring — AI-recommend, human-commit** (admin; `adminGate`, `maxDuration = 60`; migration `0021`): `q/[slug]/claim/new/page.tsx` (`ClaimForm`) and `bridge/new/page.tsx` (`BridgeForm`) create a claim / bridge-claim **and its edges** for the falsifiable, evidence-bearing nodes (frames/questions/stances are out of scope). The form's edge picker carries a relation per edge; `ClaimForm` shows a **live ghost preview** of the proposed claim on the real `QuestionMap` (a dashed `[data-proposed]` node + its edges), `BridgeForm` a proposed-styled card. The submit (`createClaimAction`/`createBridgeAction` → `createClaimWithEdges`/`createBridgeWithEdges`, which resolve edge codes→ids in one transaction and **refuse a dangling edge** since `edges` has no FK; a claim must bear on ≥1 stance) is the only writer; new nodes start at NEUTRAL 0.50 (the first `ConfidenceEditor` move records the why). "✦ AI suggest" buttons (`recommendClaimEdgesAction`/`recommendBridgeFeedersAction`, recommend-only) suggest the wiring. The map's gap panel's *Start draft* opens these forms with `?gap=<code>` to **prefill server-side from the persisted `argument_gap_scan`** (only the code travels in the URL). The proposer (`lib/argument-gaps.ts`) is **report-grounded** (it reads the latest `reports` + recent published `signals`, especially those with no `claim_touches`) and **restraint-biased** (every rec must cite its grounding or is dropped; cap 3; novelty check; recommending nothing is a normal outcome). Both forms render the `AdminOnly` notice for a non-admin; the proxy carries no page gate.
- `lens/page.tsx` — **redirect stub → `/map`** (the lens hub + per-lens views retired 2026-08-13; lens *tagging* lives on via `node_lenses` and the /data tagger). Likewise `landscape/page.tsx` → `/blotter` and `supply-chain/page.tsx` → `/traceroute`; the proxy is open by default, so sessionless visitors get the redirect.
- **Concepts — the semantic scaffold** (public): `concepts/page.tsx` — a layered dependency graph of AI terminology (`ConceptGraph`: hand-rolled SVG DAG, foundational at the bottom, hover tooltip, narrow-screen layer-list fallback); `concepts/[slug]/page.tsx` — a concept (settled/contested panel, prerequisites / builds-toward, linked claims with confidence stripped for guests). Admin authoring: `concepts/new/page.tsx` + `concepts/[slug]/edit/page.tsx` (`ConceptForm` with AI-suggest for prerequisites and claim wiring — recommend-only, the admin confirms each; the form submit is the only writer). Both `adminGate`; `maxDuration = 60`. `/concepts` also hosts the admin **gap diagnosis** (`ConceptGapPanel` + `diagnoseConceptGapsAction`, so the page sets `maxDuration = 60` too): one bounded model call (`timeoutMs 55s`, `maxRetries 0` — the report-generation lesson) reads the scaffold + the Argument Map and argues for missing concepts; the scan persists in the `concept_gap_scan` singleton (`0018`, mirrors `dedupe_scan`), is reconciled against live slugs on read, and each recommendation's *Start draft* opens `/concepts/new?gap=<slug>` which pre-fills the form **server-side from the persisted scan** (only the slug travels in the URL).
- **Research Portal** (`research/*`, public; **insight-first since the 2026-08-14 rework**): `research/page.tsx` is the reading surface — a "New this fortnight" strip (thread synthesis revisions with `trigger_note` via `getRecentThreadRevisions` + newly tracked papers via `getTrackedSince`), threads as rich cards with synthesis excerpts, an "On the Argument Map" advisory rollup (`getResearchTouchRollup`), then watchlist + noted (`getNotedPapers`, fetched directly); admins get a desk strip only. The working console moved to **`research/console/page.tsx`** (admin, `adminGate`): runs, review queue, add form, thread tools, citations, run history, plus the **findings-coverage strip** (`getFindingCoverage` + `FindingCoveragePanel`: batch "Analyze missing" over the reviewed shelf, reusing the per-paper hydrate/analyze actions, ~$0.10 each) and the **queue agent** (migration `0033`, `QueueAgentPanel` + `lib/research/queue-agent.ts`): a recommend-only pass over ALL pending papers in 12-paper `runStructured` chunks (feature `research_agent`; the run-static system block carries the standing steering note from the `research_agent_prefs` singleton, a revealed-taste digest of past tracks/notes/dismissals, and the Atlas digests), stamping each paper with `agent_recommendation/reason/confidence/cluster`; the queue re-sorts track-candidates-first, cards show ✦ agent chips, ★ Track prefills the agent's why, and the panel's per-group BULK accepts flow through the same review write (accepting tracks chains straight into finding extraction). Re-runs refresh every pending paper with current steering; recommendations are kept after decisions (future agreement-rate audit). Landmine: the Anthropic tool validator rejects `minimum`/`maximum` on integer schema properties, so ranges live in descriptions and writers clamp. `research/[id]/page.tsx`: **the FINDING is public** (editorial like signal briefs; analyze button, model-suggested rigor, review controls, rigor, promotion, raw_content stay admin), "Bears on" codes link to their claim/bridge pages, and the header carries **`PaperReader`** ("Read the paper here" + "Open on arXiv ↗" buttons; the drawer iframes the ORIGINAL arXiv document in a right side pane — arXiv's `/html/<id>` LaTeXML version by default with a PDF toggle, both frame-embeddable unlike its `frame-ancestors 'none'` abs pages; `.pv-*` CSS block). `research/threads/[slug]/page.tsx` (synthesis + papers public, now with effect-size lines; update button, `why` rationales, revisions admin). Claim/bridge pages carry a "Research bearing on this · advisory" pane (`getPapersForTarget`; papers never write evidence). `research/digest` stays admin-only (Header-less, so it redirects a non-admin to the public parent). **Papers + threads are in the /ask retrieval corpus** (migration `0031` FTS): threads are skeleton citizens cited `[thread <slug>]`; papers mint `P`-tags on the SAME counter as signal `S`-tags (one `signalOffset`, one tag map, prefix = kind; `/research/<uuid>` vs `/signals/<uuid>`), with peek panels, deep-research search/fetch, and verify-route support. **Research 2.0 (2026-08-31, mig `0046`; the manual-runs doctrine is DEAD)**: research is now the 4th daily engine — `lib/research/engine.ts` (the scan pattern: day-keyed `research_runs` rows + lease + notes, four weekday cron windows on `/api/cron/research` (10:00, 11:50, 13:40, 15:40 UTC; `/sweep`, `/sweep2`, `/sweep3` stubs), Monday 3-day lookback, `checkResearchBudget` vs `RESEARCH_DAILY_BUDGET_USD`) advancing pull → triage → agent → analyze (agent-positive papers auto-hydrate + extract, most-confident first, `papers.analyzed_by` stamps). **No Sonnet anywhere in the loop**: all three model features route through `lib/research/model-route.ts` (`researchStructured`: Anthropic ids via runStructured, else OpenRouter via `lib/scan/llm.ts`; one bounded retry on the OpenRouter path) on the `research_prefs` picks — **GLM-5.3-flash everywhere**, chosen by measurement. **The triage CHARTER** (in `lib/research/triage.ts`, ratified 2026-08-30): keep-worthiness is DEFINED by the argument graph (7 A-families + 3 B-families + explicit rejects + the "would the fortnight report cite it" tie-breaker), not taste; the regression test is the 120-paper Fable-labeled gold set in `private/research-gold/` replayed via `scripts/ab-research-triage.mjs` (GLM+charter measured 90.2% gold-keep recall at 5.1% noise; the old prompt's "few percent" anchor made every cheap model over-reject, and truncation at maxTokens 4000 masqueraded as harshness — triage now runs 8000/90s). **The Friday roundup** (`lib/research/roundup.ts`, cron `/api/cron/roundup` Fri 21:00 UTC): refreshes threads that gained papers, builds a guest-safe `RoundupPack` (never review_note/raw_content), two Sonnet narrative legs (`roundup_sections`/`roundup_close`), citation-gated, saved as `generated_reports` kind `'roundup'` — **the ONE report kind that auto-publishes** (Kevin's explicit decision); renders on /reports + `/reports/sheet/[id]` + its PDF (no next.config change), with a "This week" strip + past-roundups list on /research; quiet weeks skip. **`research-export` dataset** (key-gated): tracked+noted papers with flattened extraction fields + retained `full_text` (+ `rigor_prior`, a scoped KEY_GATED_ALLOWED exception) + `buildResearchHandoff` orientation doc; console carries downloads, the engine strip + tick loop, enabled toggle, model pickers, `getResearchHealth(30)`, run-notes issues feed, and the Model A/B table over `analyzed_by`.
- **Startup Scout** (`scout/*`, the 7th portal, 2026-08-15; migration `0034`): the acquisition-target funnel. `scout/page.tsx` (public): tracked companies grouped by vertical + a recent-activity strip; **the agent layer (verdict/scores/reasons), review notes, the queue, and non-tracked companies are admin-only and never SELECTed for guests** (a public "pursue" chip on a named startup would disclose M&A intent). `scout/[id]/page.tsx`: profile (facts, ai_tech, events timeline); public ONLY once tracked, else 404 for guests; admin gets the dossier panel, agent read, review controls (Track requires a why; ★ prefills the agent's reason), fact edits (human text always wins), enrich button, and provenance. `scout/console/page.tsx` (admin, `adminGate`, `maxDuration = 60`): discovery runs (`ScoutConsole` drives one vertical query batch per invocation over `lib/scout/web.ts` `searchCompanies` — the pipeline's web_search call shape with a youth screen: founded ≲6y, pre-seed–Series B, no fit judgment; `insertCompanies` dedupes GLOBALLY by domain + `name_key`, never re-queues a dismissed company), the **scoring agent** (`lib/scout/agent.ts`, the 0033 queue-agent pattern: 10-company runStructured chunks, feature `scout_agent`, pursue/watch/pass + five 1-5 rubric scores; rubric editable in `scout_prefs`, `DEFAULT_RUBRIC` fallback — imported by the console page as a PROP to the client panel, never into a client component), the pursue-first queue, manual adds, and the verticals manager (`scout_verticals` registry rows carry the discovery query templates, `{year}`/`{month}` tokens). Enrichment (`lib/scout/enrich.ts`, feature `scout_dossier`): homepage fetch via `fetchCandidateText` + one structured skeptical read, fill-only-null facts + `dossier` jsonb. Pure core in `lib/scout/core.ts` (Node-testable; `scripts/test-scout.mjs`). Admin nav row "Scout" with a queued-count badge. **The portal tier + research tools (2026-08-15 evening; migration `0035`)**: portal keyholders see tracked + queued profiles (PUBLIC columns only; `getCompany(id, {admin, portal})`), can **add targets** (`addTargetAction`, `requirePortal` gate, adds land queued with a "review queue" strip on /scout) and run the **ResearchPanel** on any visible profile: a one-off steering textarea (never persisted) + **web intel sweep** (`lib/scout/intel.ts`, Sonnet + web_search ×3, ~$0.10) + **document upload** (browser unpdf extraction, text retained in `company_documents`, `lib/scout/docread.ts` structured read, Re-extract on stall) + admin-only **competitor scan** (`lib/scout/competitors.ts`, queues similar young companies, provenance note event on the source) and homepage-dossier refresh. Write discipline everywhere: facts fill-only-null, the **dossier MERGES monotonically** (`mergeDossier` in core.ts; `updateCompanyEnrichment` = `fillCompanyFacts` + `mergeCompanyDossier`, three writers can't clobber each other), events dedupe by URL/normalized title with NEUTRAL public notes (no filenames: event notes render to guests on tracked profiles). Gate template: portal actions re-check target visibility with a generic error (no probing for dismissed companies) then the daily budget; portal calls log `portal_scout` (summed by `checkPortalBudget` beside `portal_ask`), admin calls log `scout_intel`/`scout_doc`/`scout_competitors`.
- **Signal Board** (public): `signals/page.tsx` — the feed (lens + significance filters; guests see published, admin also sees drafts); `signals/[id]/page.tsx` — a signal (touched claims/bridges; drafts 404 for guests; admin publish/edit/delete).
- **Signal Board admin**: `signals/new/page.tsx` + `signals/[id]/edit/page.tsx` (create/edit via `SignalForm`); `signals/drafts/page.tsx` (**the backlog desk since 2026-09-22, mig `0055`**: `DraftBacklogBar` with three judgment-free cuts that ARCHIVE, never delete (`archiveDraftsBulk`: no claim touches / low significance / older than 45 days; `signals.archive_reason` records why), then `DraftSprint`, a one-draft-at-a-time review (P publish, A archive, S skip, ← back; touches shown with statement + direction + reason via `getSprintDrafts` + `getTargets`), then the old `DraftQueue` list + dedupe scan behind a details block). **The promotion policy** (Kevin's option B): `pipeline_prefs.auto_publish_high` (default on) + `auto_publish_after_hours` (48) + `auto_publish_from`: `publishDueDrafts` publishes high-significance pipeline drafts with ≥1 claim touch that are still active (archive = veto) and were created at or after the policy start, so the pre-existing backlog stays a human sprint; it runs on every `/api/cron/pipeline` window (before the run logic, so also when the day's run is already complete) and from the bar's "Run now"; `signals.auto_published_at` stamps policy publishes. Low-significance pipeline drafts archive at intake (`low_at_intake`) inside `createDraftForCandidate`. **Nothing the crons pull is ever deleted**: the dedupe "discard" archives (`dedupe_discard`) instead of deleting the row, a dedupe merge archives the losers as `dedupe_merge` (their rows and candidate links survive; `mergeDraftSignals` works on unpublished drafts only), and archive is reversible for unpublished rows (`setSignalArchived(id, false)`; a published signal never carries `archived_at`); `signals/digest/page.tsx` (print-friendly digest, `?since/&lenses/&significance`, not in nav; Header-less, so it redirects a non-admin to the public parent). The rest `adminGate`.
- **Reports**: `reports/period/page.tsx` — admin **period-report generator** (`ReportGenerator` + `ReportPreview`; moved here from `/report` 2026-08-14, which is now a param-preserving redirect stub): pick a date range + lenses, `buildReportData` assembles the data half, then `lib/report-generate.ts` runs one `runStructured` narrative call per lens plus a synthesis pass (analyst voice, markdown→HTML via `marked`), the admin edits and saves (`saveReportAction` → `reports` table) or exports to PDF (`ReportPrint`). `adminGate`; `maxDuration = 60`. `reports/page.tsx` — **the Report Portal** (public; **the cover grid since 2026-09-22**): every report family (generated sheets incl. roundup + `tooling_*`, period reports, thesis reports) in ONE five-wide grid (`components/reports/ReportGrid.tsx`, client; 4/3/2/1 columns at 1279/1023/767/479px, 15 per page), each card a **cover-page preview** (`ReportCover`: a CSS miniature of `PdfCover` in `lib/pdf/shell.tsx`, paper-white in both themes because it previews a PDF, `cqw`-scaled inside a `container-type: inline-size` box, DRAFT ribbon for unpublished) with kind kicker, title, subject, stat chips, date, PDF button, and admin publish/unpublish/delete beneath. Search (title/subject/kind/abstract/chips substring), kind chips (All · Claim · Bridge · Lens · Executive briefing · Research roundup · Tooling · Period · Thesis, plus Drafts for admin), and the pager run **client-side over the server-assembled card set**, mirrored to `?q=&kind=&page=` via `history.replaceState` (the page reads the same params to seed SSR). The projection is pure: `lib/reports/cards.ts` (`toSheetCard`/`toPeriodCard`/`toThesisCard`, `filterCards`, `paginate`, tested by `scripts/test-reports-cards.mjs`); `listGeneratedReports` still projects only `narrative->>'bottomLine'` + `pack->'stats'`/`'health'` (guest-safe) and gained `{ toolingDraftsForPortal }` so keyholders see tooling drafts here too. Admin's SheetConsole generator collapses into a "Generate a report" `<details>` (open when `?generate=` is present); `SheetRow` survives only on the single-sheet read view. **The 16:9 decks are the fourth family** (`lib/reports/decks.ts`, a static registry: the admin-only cost deck `/costs/deck` and ingestion "1000x" deck `/ingestion/deck`, whose numbers rebuild on open, plus the public education guide decks): `visibleDecks(admin)` filters admin decks server-side, `toDeckCard` gives them kind `deck`, a landscape dark-framed `DeckCover` title-slide preview with a "16:9 deck" tag, an Open button beside PDF, a "Decks" chip, and `sortDate ''` so `sortCards` places them after every dated report. Styles `.rp-*` in `app/styles/reports.css`; `maxDuration = 60`); `reports/sheet/[id]/page.tsx` + `.../pdf/route.ts` — generated-report read view + branded PDF (public once published, admin as draft, else 404); `reports/[id]/page.tsx` + `.../pdf/route.ts` — **public** read-only view of a saved period report (`ReportReadView`, HTML re-sanitized on render) + branded period PDF (`lib/pdf/period-doc.tsx`; public, period reports have no publish gate); `thesis-report/[id]/pdf/route.ts` — public branded thesis PDF (linked from the report header's primary Download button, the console's save-success line, and each Saved-runs row).
- `calibration/page.tsx` — admin: the **confidence-history viewer** (`CalibrationView`): snapshot time-slider, per-claim trajectories, and the move log (rationales + cited evidence), plus a "Capture snapshot now" button (the still-wired `snapshotAction('manual')`). `adminGate`.
- `costs/page.tsx` — admin: the **AI cost console** (`CostsDashboard` + `RateCardForm`): every Anthropic call from `ai_cost_log` priced via `ai_rate_cards` (spend, tokens, latency, context use; `lib/cost.ts` is the meter, rate frozen at call time). `adminGate`.
- `pipeline/page.tsx` — admin: the **discovery-pipeline console** (`PipelineConsole`): discover (lens batches + breaking sweep)→triage→analyze→review over `pipeline_runs` + `signal_candidates`, then an advisory **coverage check** whose result renders as a covered/possible-miss panel on the page, plus the **retained-text coverage strip** (`TextGuardPanel` + `refetchMissingTextAction`, ≤5 rows/call). **Pipeline 2.0 (mig `0042`, full write-up `docs/discovery-pipeline-2.md`)**: cheap providers with Sonnet fallbacks (lens discovery = LLM-free Tavily via `lib/pipeline/search.ts`; sweep + coverage = Tavily fetch + one utility-model judgment call; triage = the utility model, `DEFAULT_UTILITY_MODEL` in config.ts or `pipeline_prefs.utility_model`), **analysis A/B** (the /pipeline picker's OpenRouter models per candidate via `pickEnrichModel`, drafts stamp `signals.drafted_by` — admin-only, stripped in `getSignal`, shown as a badge on the signal page + compared in the /pipeline Model A/B table; empty selection = Sonnet), **daily rotating queries** (`rotatedQueries` — ~2/lens/day over the full lists, full coverage every ~3 days; the scan's 2-query topics rotate to 1/day to stay inside Tavily's free tier), and the **server-side engine** (`lib/pipeline/engine.ts` `advancePipelineRun`, the scan pattern: `discovered_units` checkpoints + `lease_until` on `pipeline_runs`, `checkPipelineBudget` vs `PIPELINE_DAILY_BUDGET_USD` default 1.00) driven by its own weekday cron `/api/cron/pipeline` (+ `/sweep` — split out of the once-shared scan route on 2026-08-30 when Vercel Pro lifted the 2-cron cap; prefs toggle unchanged; hydrate logic shared via `lib/pipeline/hydrate.ts`). The pipeline's "day" starts at 06:00 UTC, not midnight (`PIPELINE_DAY_START_SQL` in `lib/pipeline/config.ts`): a daily run created between 00:00 and 06:00 UTC counts as the previous day, so a late-evening ET smoke test of the cron endpoint can't consume the next morning's run key. `adminGate`; `maxDuration = 60`.
- `scan/page.tsx` — admin: the **External Scan console** (migration `0038`; full write-up `docs/external-scan.md`): a weekday cron-driven sweep of public news across configurable topics (financial services + technology, broader than the AI lenses) — free RSS/Atom press feeds per topic (`lib/scan/feeds.ts`, hand-rolled parser in `lib/scan/core.ts`, Node-tested by `scripts/test-scan.mjs`) plus per-topic news search — **Tavily's LLM-free API when `TAVILY_API_KEY` is set** (`lib/scan/search-tavily.ts`, free tier, $0 cost-log rows as model `tavily-search`), else the original Sonnet `web_search` call (`lib/scan/web.ts`, the scout call shape) — hydrated via `fetchCandidateText`, then enriched per item on the **/scan model picker's choice** (migration `0041`: `scan_prefs.enrich_models` + `scan_items.enriched_by`; curated OpenRouter open-weight shortlist in `lib/scan/models.ts` with rate cards, OpenAI-compat fetch client `lib/scan/llm.ts` needing `OPENROUTER_API_KEY`; 2+ models picked = deterministic per-item A/B split, compared in the /scan "Model A/B" table; none picked = **`claude-haiku-4-5`** via `runStructured`'s `model` override) (`lib/scan/enrich.ts`: summary, taxonomy-code tags allow-listed from active topics, entities, advisory relevance) under the `SCAN_DAILY_BUDGET_USD` cap (`lib/scan/budget.ts`). The step engine (`lib/scan/run.ts`) is DB-checkpointed per unit on the day-keyed `scan_runs` row (lease column = overlap guard); drivers are the four WEEKDAY Vercel cron windows (09:00 / 11:00 / 13:00 / 15:00 UTC, the `/sweep`, `/sweep2`, `/sweep3` path stubs) hitting `/api/cron/scan` (scan-only since the 2026-08-30 Pro split; Bearer `CRON_SECRET`, `maxDuration = 300`) and the console's tick loop (`scanTickAction`, one bounded unit per call); the discovery window is `lookbackDays(day)` (`lib/scan/core.ts`: 1 day, 3 on Mondays — the weekend catch-up), and the health panel/day grid treat runless weekends as scheduled off, not misses. Topics are DB rows (`scan_topics`), seeded from the **untracked** `private/scan-topics.json` by `db:seed:scan` — the real topic set never enters the public repo. **Source reliability tiers (mig `0052`, 2026-09-02)**: `relevance` is topic fit only, so reliability is a SEPARATE, source-derived axis: `lib/scan/source-tiers.ts` (pure, Node-tested) rates a domain by suffix rules (`.gov`, `.edu`, central banks, blog platforms) and a curated map (~180 domains: regulators, research houses like Ipsos, wires, majors, trade press, tech press, aggregators, PR wires, social, promo/junk); a domain neither covers is rated ONCE by the utility model (`lib/scan/source-rating.ts`, feature `scan_source_rating`, ~40 domains per call at the search→hydrate transition of both the scan and intel engines) and persisted in `source_tiers` (`rated_by` model, a human row is never overwritten). Items are stamped `source_tier` (1 best to 4 junk) + `source_kind` at collection time (`stampSourceTiers`), `content_kind` (news/analysis/data/press_release/marketing/opinion/other) rides the existing enrichment call, and the datasets ship all three plus `priority` = relevance × tier weight (1.0/0.85/0.6/0.25) × content weight (press_release 0.7, marketing 0.4). Kevin's rule: nobody tunes reliability by hand; the curated map is editorial judgment written once, the model fills the long tail (`scripts/backfill-source-tiers.mjs` for existing rows). **The relevance ensemble (mig `0053`, same day)**: the three flash models score the anchored rubric on different rulers (30-day means on randomly split items: deepseek 0.58, qwen 0.56, GLM 0.44), and Kevin chose to KEEP the random A/B split and ensemble the score: after enrichment every other panel model (`ensemblePanel` in `lib/scan/ensemble.ts`: the picked enrich models when 2+ are picked, else the default trio) gives a score-only read of the same clipped text (`lib/scan/relevance-vote.ts`, feature `scan_relevance_vote`, ~60 output tokens), `relevance` becomes the MEDIAN of `relevance_votes` with `relevance_spread` as the disagreement signal, the A/B table shows each model's bias vs the median, and the datasets ship both new columns (`scripts/backfill-relevance-votes.mjs` for existing rows). The rubric's absolute anchors survive (a median of three readings of the same rubric), which quantile-normalizing would not have kept. The day's items publish ONLY as the key-gated **`external-scan` dataset** (heavy, `?day=` filter; the datasets route gained generic `day` plumbing). Its sibling **`signals-export`** (key-gated, on /scan's "Firewall export" panel with its own `buildSignalsExportHandoff` copy button) ships EVERY published signal in the SAME row shape — first 19 columns mirror external-scan key for key so one intake ingests both — plus appended signal-native columns incl. `touch_details` (per-touch direction/reason/statement JSON; the ONE scoped exception to the datasets guest-safety ban, key-gated-only, enforced in `test-datasets.mjs`) and a composed `full_text` (writeup + article text). The console (admin nav row "Scan") is the whole operating surface: download links, schedule + config (crons rendered live from the `vercel.json` import, env status, today's spend), the **crons on/off toggle** (`scan_prefs` singleton, migration `0039` — pauses the CRON route only, manual runs bypass it; cron TIMES stay deploy-time config in `vercel.json`), the topic registry with queries/feeds expanded, the import contract rendered from the live dataset registry, and a **Copy importer handoff** button (`lib/scan/handoff.ts` `buildScanHandoff`: an ORIENTATION doc for the firewall-side intake assistant — system overview, a formal JSON Schema generated from the registry columns + the `FIELD_FACTS` nullability/enum map (test-guarded: a new dataset column must be added to the map or `test-scan.mjs` fails), field guarantees/invariants, status semantics, live taxonomy codes, intake design guidance, transport last; portal key as a placeholder). **History & health**: a contribution-style day grid (`ScanCalendar`, 17 weeks; completed cells link to that day's JSON via `?day=&download=1`, intensity by item volume, failed days in the status color, hover tooltip + aria-labels), a 30-day health panel (`getScanHealth` in `lib/data/scan.ts`: run/miss counts, fetch success, enrichment coverage, relevance, spend), a topic-yield table (dry searched topics flagged), a recent-issues feed reading the **persisted run notes** (`0040`: the engine's feed/search/budget notes append to `scan_runs.notes`, deduped, cap 40), and per-run JSON/CSV download chips. A weekday 20:30 UTC late feed sweep (`/api/cron/feeds`, `lib/feeds/late-sweep.ts`) reopens the day's completed run at step `feeds`, re-pulls the RSS/Atom feeds (no Tavily) and lets the engine hydrate + enrich the new items under the same daily budget, noted in the run's notes (one `sweepEngine` over a `SweepEngine` adapter per engine, `SCAN`/`INTEL`). `adminGate`; `maxDuration = 60`.
- **Company intel deck** (`intel/deck/*`, 2026-09-23; migs `0061`-`0063`): a weekday 16:9 deck of what each tracked company (every tier except `self`) did since the previous press time, **access-key holders and admin only** because it names tracked companies. `lib/intel/deck-pack.ts buildIntelDeckPack(day)` reads the 16:20 → 16:20 UTC window (`windowFor(day, INTEL_DECK_PRESS_UTC)`, Monday back to Friday): items (≤6, filings = `discovered_via 'edgar'` / `doc_type 'filing'`), facts (≤6), metric rows fetched in the window with the previous period's delta, a deterministic score (Σ significance + 0.5/fact + 1.0/filing + 1.5/metric), movers = top 3, quiet = active companies with nothing; logos baked once as PNG data URIs (`lib/logo-fetch.ts`, ≤12KB, 4s, 6-way; `lib/logo.ts` + `components/EntityLogo.tsx` are the shared favicon/monogram pair, `ProductLogo` delegates). `lib/intel/deck-generate.ts` is the ONE model leg (feature `intel_deck_sentence`, `intel_prefs.deck_model` default `claude-haiku-4-5`, 12 companies per call in parallel via `Promise.allSettled`): one ≤240-char markdown sentence per company + a front, each gated by `enforceCitations` against that company's own item/fact/filing urls (the allow-list maps href → href in `tagByHref`, the only way the gate counts a link as cited); a sentence with no surviving link is dropped, a failed batch loses only its sentences, and past `INTEL_DECK_DAILY_BUDGET_USD` the deck publishes deterministic. `lib/intel/deck-run.ts runIntelDeck(day)` saves `generated_reports` kind `intel_deck` (auto-published, the fourth auto-publishing kind; 23505 → skipped); `lib/intel/deck-pure.ts buildIntelDeck` → title, stat-grid, movers bullets, one `company` slide each (the one new `DeckSlide` kind, rendered in `components/costs-deck/slides.tsx` and `lib/pdf/costs-deck.tsx`; the PDF body is a fixed 296pt clipped two-column block because `flex: 1` inside the content-sized page body measures 0 tall), quiet bullets, divider. Routes: `/intel/deck` (redirect to latest or `/intel/deck/none`), `/intel/deck/[day]` (chromeless `DeckController`; guests see a "Keyholders only" plate linking `/datasets/request`), `/intel/deck/[day]/pdf` (404 for guests; in `next.config.ts outputFileTracingIncludes`). `/reports` hides the card in SQL for guests (`PORTAL_ONLY_KINDS` in `lib/reports/access.ts`, `listGeneratedReports({ portal })`), the sheet routes 404 it, the nav leaf `Intel deck` under Report Portal is `access: 'portal'`. Cron `/api/cron/intel-deck` 16:20 UTC weekdays (`?day=` backfills); agent check `reports.intel_deck_missing` + propose remedy `reports.intel_deck`. Tests `scripts/test-intel-deck.mjs`.
- `intel/page.tsx` — admin: the **Intel Desk console** (migration `0043`; full write-up `docs/intel-desk.md`): a company-intelligence registry (tiers: self / card_issuer / consumer_bank / fintech / tech_platform / wildcard; the real registry seeds from the **untracked** `private/intel-companies.json` via `db:seed:intel`) swept daily by a checkpointed engine (`lib/intel/engine.ts`, the scan pattern: feeds → Tavily search on a 3-day company ring → SEC EDGAR filings (+ Monday metrics: EDGAR XBRL / FDIC / CFPB, LLM-free) → hydrate → cheap-model enrich with **structured fact extraction** into `intel_facts`, Monday runs closing with a per-company **dossier synthesis** via Scout's `mergeDossier`). Company linkage + dimension tags allow-listed (deBracketed first); `INTEL_DAILY_BUDGET_USD` guard; driver = `/api/cron/intel` (+`/sweep`, `/sweep2`, `/sweep3`: 09:40 / 11:40 / 13:20 / 15:20 UTC) weekdays. Output ships ONLY as four key-gated datasets (`intel-items` mirrors `external-scan`'s leading columns key for key, including the 0052 trailing `source_tier`/`source_kind`/`content_kind`/`priority` quartet, plus `intel-companies`/`intel-facts`/`intel-metrics`; `intel-metrics` takes `?since=` and `?source=` for incremental pulls) with a `buildIntelHandoff` orientation doc (`lib/intel/handoff.ts`; shared schema machinery in `lib/datasets/handoff-shared.ts`). The console carries downloads + handoff, the cron toggle + model picker, the **Tavily quota tile** (`getTavilyQuota`: month-to-date actual query count across scan/pipeline/intel vs `TAVILY_MONTHLY_CAP`, warn past 85%), the tier-grouped registry (toggles, dossier refresh), day grid, health tiles, model A/B, per-company yield, run history. The Scout bridge: `promoteScoutCompanyToIntelAction` graduates a tracked discovery into the registry. A weekday 20:30 UTC late feed sweep (`/api/cron/feeds`, `lib/feeds/late-sweep.ts`) reopens the day's completed run at step `feeds`, re-pulls the RSS/Atom feeds (no Tavily) and lets the engine hydrate + enrich the new items under the same daily budget, noted in the run's notes. `adminGate`; `maxDuration = 60`.
- **AI Tooling Monitor** (`tooling/*`, the 8th portal, 2026-09-17; migration `0054`; full write-up `docs/tooling-monitor.md`): a market scanner for AI tools built for an AI-transformation team at a large regulated financial-services company. `tooling/page.tsx` (public): FTS + facet search over the catalog, "New this week", cataloged products by category; portal keyholders also see held-for-review (parked) products, the agent read, and an add form. **Demo-prep pass 2026-09-22**: a round "i" button beside the title opens `ToolingInfo` (the page explainer: funnel, reading a card, who sees what, reports; the `.fb-overlay`/`.fb-card` dialog idiom), a mono **cadence line** under the lede (`getToolingRuns(1)`: last scan day, found/cataloged, next Monday 07:00 UTC), the filter plate `.tl-filters` (one toolbar row: search + a `<select>` per facet that navigates on change via `router.push`, "Clear all"; category headings in the grid link to `?category=`), vendor marks everywhere (`ProductLogo`, client: Google's favicon service by `vendor_domain`/URL host, a GitHub owner avatar for repo-hosted projects, a hashed-hue monogram tile on failure; nothing stored), the strip never hides (fixed 260px cards, 3-line clamp, right-edge fade, empty copy, the entrants-report link in its head row), the count line reads "N cataloged products across M categories" or "N of M products match" (`countCataloged`), card meta is ` · `-joined with "first seen" (the scanner date, not founding), the fit chip carries a rubric tooltip, `[slug]` pages get `generateMetadata`, breadcrumb links, and `humanize()`d compliance/integration/model tags; `/tooling/reports` shows guests the published shelf above the unlock. `dateLabel` now formats bare `YYYY-MM-DD` in UTC (run days and week-endings rendered a day early in US zones). Styles `.tl-*` in `app/styles/tooling.css`. `tooling/[slug]/page.tsx` (public once cataloged, `maxDuration = 120`): facts, features, dossier, agent read + deep dive (portal), events, siblings; admin curation/facts/tools. `tooling/reports/page.tsx` (portal, inline unlock, `maxDuration = 120`): the four report kinds (`tooling_landscape`, `tooling_brief`, `tooling_entrants`, `tooling_features`) on `generated_reports`, generated by `lib/tooling/reports.ts` (two Sonnet legs, citation-gated; the brief's `internal.ourContext` renders only for admin/portal; unpublished tooling reports are readable by keyholders). `tooling/console/page.tsx` (`adminGate`, `maxDuration = 60`): engine tick loops (this week's run + the big pull), prefs (thresholds, deep-dive cap, auto-publish entrants, models, steering, editable rubric), the category registry (the real list seeds from the untracked `private/tooling-categories.json` via `db:seed:tooling`), the curation queue, runs/health/spend, datasets + handoff, model A/B. **The engine** (`lib/tooling/engine.ts`, the intel pattern on `tooling_runs` keyed `(kind, day)`, weekly day = the Monday UTC): discover (`cat:<slug>` = Tavily + HN + GitHub → cheap-model triage → `insertProducts` dedupe on `url_key` else `name_key` + `vendor_domain`; `ph`; pull-only `enum:<slug>:<leaders|emerging>` Sonnet web-search enumeration) → hydrate → enrich (GLM-5.3-flash default) → score (qwen flash, the rubric: fit 0-100 + five 1-5 dims + `steal`; candidate → cataloged at `catalog_threshold` else parked; never demotes, never touches pinned) → finish (pricing page + feed discovery) → events (vendor feeds) → deepdive (`dd:<id>`, Sonnet web_search ×3, high-fit entrants, capped) → report (weekly: the entrants report, **auto-published**, the second auto-publishing kind) → complete. Per-run budget sums `ai_cost_log` rows carrying `metadata.tooling_run` against `TOOLING_WEEKLY_BUDGET_USD` (4) / `TOOLING_PULL_BUDGET_USD` (12). Crons: `/api/cron/tooling` Mon 07:00 UTC + `/sweep` 08:20 + `/sweep2` 17:00; `?kind=pull` advances a console-started pull headlessly. Datasets: key-gated `tooling-products`/`tooling-events`/`tooling-features`, public `tooling-catalog`. Guest safety: `PRODUCT_PUBLIC_COLUMNS`/`PORTAL`/`ADMIN` column lists in `lib/data/tooling.ts`; agent fields are portal+admin only.
- **The Atlas Agent** (`/agent` + the rail orb, 2026-09-22; migration `0056`; the resident operator): four layers, all admin-only. **Sensors**: `lib/agent/checks/{queues,engines,editorial}.ts`, ~26 deterministic SQL checks (draft backlog, tooling/scout/research queues, tickets, unpublished reports, stale gap recs, coverage misses; daily-job failures, missed days, paused engines, budgets near cap, Tavily quota, retained-text coverage, zero-yield domains, missing roundup/entrants reports, run-note errors; claims with evidence outrunning their confidence, one-sided claims, dangling touch codes, stale question summaries, sources without priors, stale gap scans) filed into `agent_findings` by `runChecks` (`lib/agent/runner.ts` + the pure `lib/agent/reconcile.ts`: upsert by key, auto-resolve when a check stops firing, snooze/ack states). **A throwing check is not a quiet check**: `runChecks` catches and logs it, records its key in a `failed` set, and the reconciler leaves that check's existing findings alone instead of resolving them (the checks themselves carry no try/catch; the runner is the one boundary). **Hands**: `lib/agent/remedies.ts`, an allow-list of remedies mapped to existing library functions with tiers `auto` (reversible, no guest-visible change, never creates a run: archive no-touch drafts older than 3d, promotion sweep, refetch missing text, dedupe scan, resume a FAILED run in weekday engine hours) / `propose` (Kevin taps Do it or asks in chat: archive low/stale, kick or enable an engine, generate the missed roundup/entrants report, regenerate a question summary, gap diagnoses, research/scout agent chunks, resolve a ticket) / `never` (confidence, publishing outside the policy, merges, deletes); `executeRemedy` guards tier, actor, the 00:00-09:00 UTC blackout for run creation, `checkAgentBudget` (`agentCapUsd()` in `lib/agent/budget.ts`: `AGENT_DAILY_BUDGET_USD` 0.25, floor 0.05; today's spend is defined ONCE in `getAgentSpendToday`: `ai_cost_log` rows tagged `agent_brief`/`agent_chat` plus `agent_actions.cost_usd`, since remedies log under their callees' own features), a 12h per-finding cooldown, and logs every call to `agent_actions`. **Brain** (GLM by default, `agent_prefs.chat_model/brief_model`, `lib/agent/prompt.ts` holds the persona and standing rules): `lib/agent/brief.ts runDailyBrief` (one `routedStructured` memo per UTC day into `agent_briefs`, falling back to `buildDeterministicMemo` in the pure `lib/agent/brief-core.ts` when the budget is spent or the model fails; emailed via Resend when `RESEND_API_KEY` + `AGENT_EMAIL_TO` are set, `lib/agent/email.ts`) and `lib/agent/chat.ts` (a provider-agnostic JSON step loop, ≤4 steps, 11 tools incl. `run_remedy` for propose tier only, NDJSON from `POST /api/agent/chat`). **Surfaces**: `AgentOrb` in the rail's bottom island (badge = unread open findings, polls `GET /api/agent/pulse` every 60s) opening `AgentDrawer` (Today / Findings / Activity / Chat + prefs), high-severity toasts, the mobile menu row, and `/agent` (`adminGate`) with the check registry and briefs archive; state in one `GET /api/agent/state`. Crons: `/api/cron/agent` hourly (`45 * * * *`) and `/api/cron/agent/brief` at 12:30 UTC. Styles `.ag-*` in `app/styles/agent.css`. Tests `scripts/test-agent.mjs` + `test-agent-brain.mjs`.
- `traceroute/page.tsx` — **public**: the "Traceroute" explainer (`force-dynamic`, **no `maxDuration`**, because nothing here calls a model). Four movements over ten camera stops, a three.js world scene per movement plus a silicon scene for the transformer stack, and a scripted autoregressive walkthrough with play/pause/step/scrub. Data is pure modules under `lib/traceroute/*` (stops, props, architecture, runs, compiler, player, camera, geometry); UI is `components/traceroute/*`. Its one API route, `app/api/traceroute/tokenize/route.ts`, is public and is **pure string processing over a length-capped input, not a model call**. (The "no public surface can trigger an Anthropic call" property now has exactly one deliberate exception: the key-gated, budget-capped `/api/portal/ask`.) That route is on the `proxy.ts` API allow-list (the page, like every page, is public by default). Full write-up: `docs/traceroute.md`.
- **Data Portal** (public; `docs/data-portal.md`): `datasets/page.tsx` — the catalog hub; `datasets/[slug]/page.tsx` — dataset page (auto-generated schema table from the `lib/datasets/registry.ts` column defs, methodology, downloads, `DatasetExplorer` client island; the key-gated `articles-full-text` previews only for portal holders); `datasets/ask/page.tsx` — redirect to `/ask`; `datasets/enter/route.ts` — one-link onboarding (`?k=<key>` → portal cookie → `/ask`; a lapsed key bounces to `/datasets?key=expired|revoked`); `app/api/datasets/[slug]/route.ts` — the CSV/JSON download route (UTF-8 BOM, `Content-Disposition: attachment`, CDN cache for public datasets, in-route identity gate for the key-gated ones, a `portal_usage` row per keyed pull written in `after()`); `app/api/portal/ask/route.ts` — the portal Ask endpoint (`maxDuration = 60`, Haiku, the shared daily budget then the per-key one via `lib/portal/budget.ts`, guest-safe retrieval mode, feature slug `portal_ask`). **Per-person access keys (mig `0060`, 2026-09-23)**: `atlas_<prefix8>_<secret32>`, HMAC-SHA256 under `AUTH_SECRET`, prefix lookup + `timingSafeEqual` (`lib/portal/keys.ts` pure, `scripts/test-portal-keys.mjs`); shown ONCE at issuance. `lib/portal/identity.ts` is the one resolver (`getPortalIdentity()` for pages/actions, `identityFromRequest(req)` for route handlers): admin cookie, the `atlas_portal` cookie (`portal:<keyId>:<exp>`, legacy `portal` still valid), or a header (`Authorization: Bearer <key>` / `X-Atlas-Key`, the legacy `PORTAL_KEY` accepted there too so intake scripts need no cookie); `isPortal()` is AUTHORITATIVE (DB lookup per request, React `cache()`), so revocation and expiry bite immediately; 401 bodies name the state (`key_expired` / `key_revoked` / `key_required`, `X-Atlas-Key-State` header) and pages render `components/portal/RenewalNotice.tsx`. Public request form `datasets/request/page.tsx` → `POST /api/access/request` (tickets-route discipline: honeypot, per-IP limiter, caps, email regex, domain allow-list from `PORTAL_REQUEST_EMAIL_DOMAINS`; **503 closed while that env is unset**) → `portal_access_requests` row + Resend email (`lib/email/resend.ts sendEmail`, shared with the agent brief; to `PORTAL_NOTIFY_TO` → `AGENT_EMAIL_TO` → `agent_prefs.email_to`) + Admin desk badge (`getNavCounts().access`) + agent findings `access.requests_pending` / `access.keys_expiring`. Admin console **`/access`** (`adminGate`; `components/access/*`, `lib/actions/portal.ts`, `lib/data/portal.ts`, `lib/mutations/portal.ts`): pending requests (Approve = issue prefilled, Decline), keys (renew = `greatest(expires_at, now()) + 90d`, budget, revoke), usage tail. Per-key Ask spend = one indexed `ai_cost_log` sum over `metadata.portal_key_id` (every portal `recordApiCall` stamps it, the lane classifier via `classifyMetadata`). Reader-facing copy says "access key", never "team key". **Data Portal 2 (2026-09-24, Phases 3-5 of the program)**: the hub is `components/datasets/DatasetCatalog.tsx` (client) over pure `lib/datasets/cards.ts` (the `.tl-filters` plate: search over title/description/slug/column keys, Access, Category, Clear all; count line; category kickers when filtered; locked key-gated cards show Schema / Query / Unlock / Request access, never a CSV/JSON button that would 401; URL-mirrored via `replaceState`; `scripts/test-dataset-cards.mjs`). **The query grammar** on `/api/datasets/[slug]` lives in pure `lib/datasets/filter.ts` (`parseFilterSpec` / `applyFilterSpec` / `projectColumns` / `guardFilterRequest`, `scripts/test-dataset-filter.mjs`): `where=<col>:<op>:<value>` (max 8; ops by column type, enum values validated against `DatasetColumn.values ?? FIELD_FACTS`, `in` lists of 20, values capped at 200 chars, `isnull`/`notnull` take no value), `cols=` projection (CSV header and JSON `columns` follow it), `sort=` (2 keys + a stable first-column tiebreak, nulls last), `limit=` 1..50000 after filter/sort, `q=` over text/longtext/enum columns, `schema=1` (the slice's JSON Schema, no DB call), the SQL pushdowns `lens|day|since|source|company` (`company=<slug>` on the three intel sets only), and `view=<uuid>` (a saved view's stored param record merged UNDER the explicit params; the gate runs first, so a view never widens a dataset). Everything but the pushdowns post-filters in JS over the builders' deterministic rows, so builder SQL never changes and no column can be added. Guardrails: intel-metrics needs `since|source|company` before any JS-side filter or sort, 413 past 400,000 built rows on a filtered request; `preview=N` with `where`/`q` filters the full slice then caps; a filtered filename ends `-filtered`; the JSON envelope carries `dataset.filter`; keyed pulls log the spec and the SERVED row count. Landmine: `FIELD_FACTS` in `lib/datasets/handoff-shared.ts` is keyed by column NAME across every dataset, so two datasets sharing a column name with different enums collide (`concepts.status` settled|contested vs tooling's status); declare the override on the column (`col('status', ..., ['settled', 'contested'])`) rather than widening the shared enum. **The query builder** (`components/datasets/QueryBuilder.tsx`, client, on every dataset page over pure `lib/datasets/query-url.ts`: `BuilderState` <-> URL, `hrefs`, `describeState`, `requiresNarrowing`; `scripts/test-query-url.mjs` round-trips every op through the real `parseFilterSpec`): filter rows, the declared pushdowns, column checkboxes, sort, limit, search, then Preview (25 rows, only on click, AbortController) / Download CSV / Download JSON / Copy URL / Schema / Reset; enum values are resolved SERVER-SIDE on the page (`col.values ?? fieldEnumValues`) so the client never imports the registry; a locked viewer on a key-gated set gets the Unlock / Request access links and a disabled Preview. `DatasetExplorer` is gone. **Saved views** (mig `0064` `portal_views`: owner key|legacy|admin, `spec` = the URL-param record re-validated by `parseFilterSpec` at save AND apply, `is_shared` = Team views): `GET/POST /api/portal/views`, `PATCH/DELETE /api/portal/views/[id]` (owner or admin; 404 never distinguishes existence), pure rules in `lib/portal/views-core.ts`. **Ask for data in words**: `POST /api/portal/query/nl` (`lib/portal/nl-query.ts` + pure `lib/portal/nl-core.ts`, feature `portal_nl_query` under both portal budgets, `PORTAL_NL_MODEL` or the utility model): one structured call over a compact catalog returns `{ dataset, params }` which `validateNlOutput` coerces through `parseFilterSpec`, dropping and LISTING every invalid part; never SQL, never rows; the client then calls the datasets route. **Handoffs**: every importer handoff's Transport section documents header auth, the grammar, `company=`, `view=`, `schema=1`, and the 401 states; `GET /api/datasets/[slug]/handoff` (pure `lib/datasets/handoff-generic.ts`) renders a per-dataset orientation doc, key-gated like its dataset. `/access` carries per-key usage for 30 days (`getKeyUsageSummary`) and `/costs` a Portal-by-key table; `listGeneratedReports(publishedOnly, { portal })` is the one report-visibility rule (`toolingDraftsForPortal` retired). API routes on the `proxy.ts` allow-list live in `lib/route-shapes.ts` (`PUBLIC_API_PATHS` + `PUBLIC_API_PREFIXES`: `/api/datasets/`, `/api/portal/`, `/api/cron/`, ...); `scripts/test-api-gates.mjs` walks `app/api/**/route.ts` and fails on any route without a gate marker unless it is in the test's EXEMPT set.
- **Hybrid retrieval (2026-09-24, mig `0066`; explainer at `/about/retrieval`)**: Ask's retriever (`lib/ask/retrieve.ts buildAskContext`) runs a LEXICAL leg (the FTS `search_tsv` legs, `ts_rank`) and a SEMANTIC leg: every retrievable record is embedded with `openai/text-embedding-3-small` through OpenRouter (`lib/embed/{chunk,client,sources,index}.ts`; ~800-token title-prefixed chunks, ≤12 per record, `text_hash` skips unchanged text; 11 kinds incl. `scan_item`/`intel_item`/`intel_fact` for keyholders + admin only; `embeddings` table, HNSW cosine, RLS) and the question is embedded once per turn (`embed_query`). `ask_prefs.retrieval` (`fts` | `hybrid`, **hybrid since 2026-09-24**) decides whether the vector order is fused with the FTS order by reciprocal rank fusion (`lib/ask/fusion.ts`, k = 60, the FTS top 3 always kept inside the first 10); `maxSim` is ALWAYS computed and `decideLane` (`lib/ask/lanes.ts`) uses the sim bars `STRONG_SIM 0.55` / `MID_SIM 0.45` / `WEAK_SIM 0.42` beside the rank bars (measured 2026-09-24 on the 40-question gold set in `private/ask-gold/`, replayed by `npx -y tsx scripts/ab-retrieval-run.mts` with `ASK_RETRIEVAL_OVERRIDE=fts|hybrid`: recall@10 54% -> 86%, intel questions 0% -> 67%, lane agreement 42% -> 82%; one known miss, p05, a weak lexical hit on B4 buried by a broad question). New records embed as they are written (`embedLater` in every writer, `EMBED_DAILY_BUDGET_USD` 0.25) and the agent check `embeddings.missing` + auto remedy `embeddings.backfill` catch gaps. Citation tags: `I<n>` items, `X<n>` facts (`F<n>` is a frame-claim code). Landmines: `lib/*` extensionless imports do not load under plain Node, so measurement scripts run through `npx -y tsx`; block push order is NOT rank order (sort by `rank` before taking an FTS top-N); the 25-signal chunk truncation lesson applies to any structured batch call.
- **Ask workspace** (`ask/page.tsx`, public page in the main nav; the app's ONE viewport-height layout, `.ask-*` CSS block): `AskWorkspace` + `components/ask/*` render the multi-turn surface; mode by auth (`admin` → **the research loop at `/api/ask/deep` on EVERY turn** since 2026-08-21 (the old Deep toggle is gone; the quick `/api/ask` remains for the per-signal widget), `portal` → `/api/portal/ask` (the quick path, budget reasons), `locked` → inline unlock). The one composer toggle is **Web search**, wired into BOTH paths: the deep loop takes the raw `web_search_20250305` tool (max_uses 3) with a records-primary system swap (`askSystem(web)` in `lib/ask/prompt.ts` replaces the records-only scope paragraph rather than appending a contradiction, and `queryBlock`/`deepQueryBlock` carry web-aware closing instructions, the fix for the silent no-search bug), server-tool result blocks captured for the sources strip and pre-search narration text dropped (only text after the last `web_search_tool_result` block is the answer). Every turn ends with a **cost report** (`AskCost` footer: USD, tokens in/out, searches, model calls, expandable token explainer): the quick routes ride an `@@ASK_COST@@` sentinel line before the web-sources one, the deep route an NDJSON `cost` event; priced by `priceUsage` in `lib/cost.ts` (same frozen rate-card math as `recordApiCall`). Answers render **kind-tinted citation pills** (`.ask-cite[data-kind]`) and a bold-only formatting subset (`**Header**` → `<strong>`, everything else stays literal). Leans are policed as label + flag: the deep prompt requires a "One reading:" prefix plus the strongest opposing record, and the verifier's `verdict_language` field surfaces unlabeled verdicts as caution lines; with web on, missing-figure checks soften (searched page text arrives encrypted, so `VerifyReport.webSearched` marks them possibly-web-sourced instead of flagged). Retrieval is vocabulary-expanded (`expandVocabulary` in `lib/ask/history.ts` maps street terms to Atlas terms: "open source"→"open-weight", "beating"→"parity"), length-normalized (`ts_rank(..., 1)` on the long-document legs), signal/evidence-weighted (2 round-robin slots each, `MAX_DETAIL` 11k, claim→evidence expansion on the FTS path, relevance-ordered signal expansion), and the shared `ORQ` tsquery lives in `lib/pack-shared.ts` (one copy for retrieve/search/thesis-pack). Conversations live in localStorage only (`components/ask/store.ts`). Citation chips open the peek panel (`AskPeek` → public `GET /api/ask/peek`, guest-safe payloads from `lib/ask/search.ts`); on signal peeks, admin/portal readers get the **document viewer** (`AskDoc` → `GET /api/ask/doc`, allow-listed but `isPortal()`-gated in-route, retained text with question-term highlights). The multi-turn wire contract lives in `lib/ask/history.ts`; signal-tag offsets prevent cross-turn citation mislinks. `/api/ask` itself stays admin-gated and OFF the proxy allow-list; `/ask` + `/api/ask/peek` + `/api/ask/doc` are ON it. The per-signal single-shot `AskAtlas` on `/signals/[id]` is unchanged (shared renderer in `components/ask/answer.tsx`). **Deep research** (admin-only toggle in the composer): `POST /api/ask/deep` (admin-gated, OFF the allow-list, `maxDuration = 300`) streams NDJSON from a bounded tool-use loop, with an always-on two-layer faithfulness check on every answer. **Answer verification on demand**: `POST /api/ask/verify` (admin-gated, OFF the allow-list) backs the "Check this answer" button on quick-Ask answers. **Lanes (2026-09-23)**: every turn is classified in parallel with retrieval (`classifyQuestion` in `lib/ask/classify.ts`, feature `ask_classify` on the utility model) and `decideLane` (`lib/ask/lanes.ts`) combines the classifier's beat (it sees the previous user turn too, so a bare follow-up classifies on-beat) with retrieval's `hitCount`/`maxRank`/`explicit` into one of `covered`/`thin`/`adjacent`/`unrelated` (rank bars measured on the live corpus 2026-09-23: `STRONG_RANK` 0.06 covers alone, `MID_RANK` 0.03 covers when the beat is atlas, `WEAK_RANK` 0.02 is the noise floor below which an off-beat question declines; hit counts are noise, even pasta matched 24 blocks); a thin or adjacent answer orients from the records (or says plainly the Atlas does not track this) then writes a `@@BEYOND@@` marker on its own line before anything drawn from the model's own knowledge or the web (labeled as such), and the verifier only ever checks the text above that marker. The marker instruction rides in the system prompt AND as a `laneReminder` in the user turn (Haiku ignores the system-prompt copy alone); on a thin/adjacent lane with web on, the deep route routes the answer through its forced final (one extra call) because an answer written in the same turn as the searches puts web findings above the marker whatever the prompt says. The quick routes carry the lane on an `X-Ask-Lane` response header and the deep route emits it first as a `lane` NDJSON event; an `unrelated` lane short-circuits to a coded decline (`composeDecline`, no model call, no cost row), sent as an `@@ASK_DECLINE@@` sentinel on the quick routes or a `decline` NDJSON event on the deep route, and a fresh, non-covered question auto-enables web search for that turn even with the composer's toggle off. The three routes share the plumbing: `resolveLane(msgs, { mode, tagStart?, classifyFeature? })` in the server-only `lib/ask/resolve-lane.ts` (namespace + retrieval + classifier in parallel, then `decideLane`, auto-web, and the decline) plus `laneHeaders` for the quick routes' response headers; `streamQuickAnswer` in `lib/ask/quick-stream.ts` is the quick routes' whole stream leg (web tool, cost sentinel, web-sources sentinel, `recordApiCall`), also used by the per-signal `/api/signals/[id]/ask`; web sources are collected once by `createWebSourceCollector` in `lib/ask/history.ts` (stream events AND finished-message blocks); the pure lane helpers (`beatDescriptionFrom`, `questionLinksFrom`, `cleanTopic`, `parseLane`, the rank bars) live import-free in `lib/ask/lanes.ts` so `scripts/test-ask-lanes.mjs` covers them; and the one `deDash` for streamed text is exported from `lib/voice.ts`. See the Where-we-left-off item for the full mechanics.
- `sources/page.tsx` — admin: the source library (filterable list with dossier previews + evidence tallies, plus an **Evidence map**). `adminGate`.
- `ingest/page.tsx` — admin: the add-source form (PDF auto-populate). `adminGate`.
- `source/[id]/page.tsx` — admin: set reliability prior, generate the AI dossier, attach/reassign/detach evidence (with AI claim recommendations), **turn the source into a Signal Board signal** (`TurnIntoSignalButton` — routes the source as a pre-approved candidate through the same triage→analyze→draft pipeline as discovery, tagged `origin='manual'`), delete the source. `adminGate`; `maxDuration = 60`.
- `worldview/page.tsx` — admin: the spine + the **cross-cutting positions editor** (create/delete positions, attach stances/claims/bridges as components, set confidence through the human gate). `adminGate`.
- `data/page.tsx` — admin: the **domain-text editor** — inline-edit the prose of questions/stances/claims/bridges (`getAllDomainRows`, `DataField`) + per-node lens tagging (`LensTagger`). `adminGate`.
- `about/page.tsx` + sub-pages (`guardrails`, `glossary`, `limitations`, `ingestion`, **`data-handling`** (2026-09-23: who runs it, sources by engine, third-party processors, what goes to model providers, what is stored with no retention limit today, personal data, the three access tiers, which outputs are model-written and auto-published; every count derives from `NAV_TREE`/`DATASETS`) + `why-bespoke` (the 2026-08-30 associate-facing pair: the signal-ingestion explainer and the vs-chatbots/vs-market-platforms positioning, generic language, no employer or company names), plus `architecture` which is deliberately UNLISTED: it renders at its URL but appears in no nav or hub; `reading-guide` was deleted 2026-08-15, its route is a redirect stub → `/about`) — public explainer set, sharing a bare `about/layout.tsx` (the `AboutNav` rail is gone; the group's tabs come from `PageTop`); all use `Editable`/`Prose` for inline admin edit. Public like every page (the proxy carries no page gate). Rewritten 2026-08-15 to ~2,400 words, reference tone (no `content_blocks` overrides exist for `about.*`, so the code defaults ARE the live copy). **Truth pass 2026-09-23**: every public sentence about how the tool works was checked against the code by a skeptic agent; public copy never names the employer or a person ("the maintainer"), never hard-codes portal/dataset/table counts (derive from `NAV_TREE`, `DATASETS`, or say "as of migration NNNN"), and reader-facing text says "access key", never "team portal key". When automation changes (a new auto-publishing kind, a new search leg, a new processor), update `/about/data-handling` and the About set in the same commit.
- **Education hub** (`education/*`, public, 2026-08-31): a shelf of hand-kept guides ("not a feed, a shelf"), each converted to house style as code — no DB table, no upload flow. `lib/education/registry.ts` (`GuideMeta` + `GUIDES`) drives the hub (`education/page.tsx`, `.qgrid` cards + `Editable` `education.hub.*` keys); `education/[slug]/page.tsx` dispatches to a per-guide body component under `components/education/<slug>/` (v1: `agentic-harnesses`, the field guide — two client islands for the clickable spectrum grid + loop toggle, the rest server JSX; styles `.edu-*` in `app/styles/education.css`, guide-local `--edu-*` palette with a dark block). **Every guide can ship a 16:9 deck**: `lib/education/decks.ts` maps slug → a `CostDeck` builder (`lib/education/decks/<slug>.ts`, static editorial copy) served by `education/[slug]/deck` (the `DeckController` stage, public unlike `/costs/deck`) + `.../deck/pdf` (960x540 `renderCostDeckPdf`; the route is in `next.config.ts` `outputFileTracingIncludes` — fonts 500 in prod without it). The deck slide model gained a generic `bullets` kind (`lib/costs-deck.ts` + both renderers). Nav: a top-level rail link (mortarboard `NAV_ICONS.education`) + a mobile `SiteNav` link; deliberately NOT in `PORTALS` (that would auto-add a lobby tile). Adding a guide = body component + registry entry + optional deck builder.
- `showcase/page.tsx` — **public but UNLISTED** (`robots: noindex`, in no nav, chromeless via `isChromeless()`; admin nav has a link + a copy-URL button): the demo deck, `force-dynamic` with live DB stats; `components/showcase/Showcase.tsx` is the client slide controller; `.show-*` CSS block. **v2 (19 slides)**: bullets only (cover/seed/close stay statements), six portal slides pair bullets with real 2x screen grabs (`public/showcase/*.png`, recaptured via `scripts/capture-showcase.mjs`, playwright devDep; `ONLY=ask ADMIN_COOKIE=... ` for the admin ask.png money shot), two pipeline flow-diagram slides (discovery + manual ingest, human-gate node lit), and the search-vs-chatbots-vs-Atlas comparison grid. The proxy matcher excludes `.png`/`.jpg` (like `.svg`) so the grabs load sessionless. The runbook lives in `private/docs/demo-runbook.md` (untracked).
- **The feedback box** (2026-08-14): "Report a bug" / "Request a feature" dialogs opened from two buttons in the rail's bottom island (`components/feedback/FeedbackButtons.tsx`, also `variant="menu"` rows in the mobile sheet — those stopPropagation so the sheet's close-on-click never unmounts the open dialog). `components/feedback/FeedbackDialog.tsx` is the shared dialog (`.fb-*` CSS block): fun copy ("Oh no! You found a bug!" / "Ooh! An idea!"), an SVG beetle that crawls the card's top edge (legs scuttle, pauses mid-card) or an idea spark that comets across, severity chips for bugs, required email, up to 3 screenshots downscaled client-side (canvas, 1600px JPEG). POSTs multipart to the public **`/api/tickets`** (allow-listed; the trust boundary: honeypot, per-IP best-effort limiter, length caps, email regex, image magic-byte sniff, 2MB cap) → `createTicket` (the ONE public write in mutations.ts). Admin desk at **`/tickets`** (`TicketRow`: status select open/in_progress/resolved/declined, admin note, delete, screenshot thumbs via the admin-gated `/api/tickets/image/[id]`); nav row "Tickets" with an open-count badge (`getNavCounts().tickets`).
- `login/` — `page.tsx` (Admin password form + "Enter as Guest"); `actions.ts` (`loginAdmin`, `enterAsGuest`, `logout`).
- `share/route.ts` — public share-link handler (`/share?t=<token>`): verifies the token, sets a guest session, forwards on. Public (not under `/api/`, so the proxy never fences it).
- **Navigation and page tops (2026-09-23)**: `lib/nav.ts` is the ONE navigation tree (`NAV_TREE` portals + `NAV_ISLAND` About/Admin desk; leaves carry `access` public/portal/admin, badges, `also` prefixes; helpers `groupFor`/`leafFor`/`tabsFor`/`pathwayFor`/`publicParentFor`). Three surfaces read it: the rail (`PortalRail`: 56px icons, expands to 264px on hover/focus/pin, click a group for its inline accordion, admin leaves hidden from guests), the mobile sheet (`SiteNav`: `<details>` per group) and the page top. **The chrome renders ONCE, in the root layout** (2026-09-23): `app/layout.tsx` renders `<ChromeGate><Header /></ChromeGate>`, so the rail and bar persist across client navigation (they used to render inside every page and remount on each click, losing hover/pin/accordion state). Pages must NOT render `<Header>`. `ChromeGate` (client) hides it on the chromeless routes in `isChromeless()` (`lib/nav.ts`: /login, /showcase, the deck stages, the print digests); `app/loading.tsx` covers only the content area. Because the layout does not re-render on a client navigation, the chrome keeps itself current three ways: cookie-setting server actions (login, logout, preview/edit toggles, portal unlock) and every `revalidatePath` re-render it; the badge counts refetch per navigation from the admin-only `GET /api/nav/counts` (`lib/nav-counts-client.ts` is a thin hook over `createNavCountsStore` in the React-free `lib/nav-counts-store.ts`, one store shared by rail and sheet, a `fetchedFor` guard so a late fetch never overwrites fresher server counts; tested by `scripts/test-nav-counts-store.mjs`); `ViewerSync` asks the public `GET /api/nav/viewer` (on the proxy allow-list, returns only the caller's own flags from `getChromeViewer()` in `lib/chrome-viewer.ts`) on tab focus and `router.refresh()`es when the session changed in another tab, and a 401 or redirected response from the counts or agent-pulse polls does the same through ONE path: `sessionLost` + `refreshChromeOnce` in `lib/chrome-session-client.ts` (a once-per-pathname guard, `claimChromeRefresh`). The rail opens the new group's accordion when the active group changes and drops hover expansion on navigation unless the pointer is still over it; the mobile sheet and agent drawer close on navigation (the reset-on-navigation blocks use `useValueChange`/`usePathnameChange` from `lib/use-route-change.ts`, never a stored previous-path copy). The `/ask` and lobby 100dvh shells are `body:has(> .ask-page)` / `body:has(> .lobby-page)` flex columns. The header's Admin dropdown is now a slim **account menu** (Edit mode, Preview as guest, Share link, Copy showcase link, Sign out). **`PageTop`** (`components/PageTop.tsx`) is the one page-top grammar: pathway row (mono, linked segments, the round "i" at the right edge opening `PageInfo` with the entry from `lib/page-info.ts`), title row (an Editable h1 or a string, optional right-aligned action), the group's tabs when the viewer sees 2+ sibling leaves (badges from `getNavCounts`), an optional mono status line, then a hairline. No lede: the sentence moved into the info dialog's summary. Rolled out to every page with a Header on 2026-09-23; `.pagehead`/`.crumbs`/`WorkspaceTabs`/`AboutNav`/`ResearchInfo`/`ToolingInfo` are gone (only `.lede` survives for the login card). The registry is split into `lib/page-info-{a,b,c}.ts` slices merged in `lib/page-info.ts`; read views pass `title={null}` so the body keeps its own h1. **Admin gating**: pages call `adminGate(pathname, title)` (`lib/admin-gate.tsx`) as their first await and `return` the `AdminOnly` notice it yields for non-admins (a plate with "Back to <public parent>" via `publicParentFor`); the four Header-less deck/print pages redirect to the parent instead. `proxy.ts` is **open by default**: every page renders sessionless and gates itself; the proxy only fences API routes outside its allow-list (`/api/datasets/`, `/api/portal/ask`, `/api/ask/peek`, `/api/ask/doc`, `/api/tooling/events`, `/api/nav/viewer`, `/api/tickets`, `/api/traceroute/tokenize`, `/api/cron/`) and rewrites malformed detail URLs to a 404 (`lib/route-shapes.ts`; the blotter day pages reuse its `isRealDay`). Tests: `scripts/test-nav.mjs`, `test-route-shapes.mjs`.
- **Tooling table** (`tooling/table/page.tsx`, public, the "Table" tab): every cataloged product in a sortable, filterable table (`components/tooling/ProductTable.tsx`; pure `lib/tooling/table-core.ts` for rows, columns, comparators, matching; `ProductFilters` gained a `base` prop), 50 per page, column chooser, free-text row filter, compare strip for 2-4 selected rows (feature union), a quick-read drawer per row (`ProductQuickRead`: facts, features, agent read for portal/admin, last 5 events from the public `GET /api/tooling/events?product=`), CSV via the datasets route (`tooling-catalog` for guests, `tooling-products` for keyholders). Tests: `scripts/test-tooling-table.mjs`.
- `components/` — nav: `Header` + `SiteNav` (`'use client'`; Explore + Admin dropdowns, mobile hamburger — there is **no "Snapshot" nav button**). Map: `QuestionCard`, `QuestionMap` (`'use client'`), `StanceCard`, `ClaimRow`, `BridgeBand`, `HeatChips`, `ConfidenceBadge`, `ConfidenceEditor` (`'use client'`), `EvidenceList`, `EvidenceForm` (`'use client'`), `LensTagger` (`'use client'`). Sources/AI: `SourceForm` (`'use client'`), `DossierView`, `GenerateDossierButton`, `DeleteSourceButton`, `SourcesHub` (`'use client'`), `SourceEvidenceMap` (`'use client'`), `QuestionSummaryView`, `GenerateSummaryButton`. Lobby: `lobby/LobbyAsk` (`'use client'`), `lobby/WidgetBoard`, `lobby/CustomizeWidgets` (`'use client'`), `lobby/widgets/*` (the 14-widget registry + components). Signal Board: `SignalCard`, `SignalBadges` (`LensBadges`/`SignificanceTag`), `SignalFilters` (`'use client'`), `SignalForm` (`'use client'`), `TurnIntoSignalButton` (`'use client'`; source-page → signal via the pipeline), `PipelineConsole` (`'use client'`). Concepts: `ConceptGraph` (`'use client'`; layered SVG DAG), `ConceptForm` (`'use client'`). Worldview: `PositionStatementEditor` (`'use client'`). Content/share: `Editable` (`'use client'`) + `Prose`, `ThemeToggle` (`'use client'`), `ShareLinkButton` (`'use client'`), `ShareNotice`, `DataField` (`'use client'`).

## Data model (`supabase/migrations/`)
**76 tables as of `0063`** (`0060` adds the **access-key trio**: `portal_keys`, `portal_access_requests`, `portal_usage`, plus an `ai_cost_log` index on `(metadata->>'portal_key_id', created_at)`; `0061` adds no tables: `report_kind_t` value `intel_deck` + `intel_prefs.deck_enabled/deck_model`; `0062` the one-deck-per-day unique index; `0063` flips the deck model default to `claude-haiku-4-5` after GLM measured 61-77s per batch) (`0059` adds no tables: `signals.first_published_at`, stamped once by every publishing writer and read by the Daily Edition's signals window; `0058` adds no tables: the one-edition-per-day unique index) (`0057` adds `edition_prefs` and the `report_kind_t` value `edition`) (`0056` adds the **Atlas Agent** four: `agent_findings`, `agent_actions`, `agent_briefs`, `agent_prefs`) (`0055` adds no tables: `signals.archive_reason` + `auto_published_at`, and the promotion policy columns on `pipeline_prefs`: `auto_publish_high`, `auto_publish_after_hours`, `auto_publish_from`) (`0054` adds the **Tooling Monitor** five: `tooling_categories` (registry; the real list seeds from the untracked `private/tooling-categories.json`), `tooling_runs` (keyed `(kind, day)`, lease + swept units + notes), `tooling_products` (library + funnel state, `url_key` unique partial, generated `search_tsv` via the IMMUTABLE `tooling_array_text()`), `tooling_events`, `tooling_prefs`; plus four `report_kind_t` values `tooling_*`; see `docs/tooling-monitor.md`) (`0053` adds no tables: `scan_items.relevance_votes` + `relevance_spread`, the relevance ensemble) (`0052` adds `source_tiers`, the model-rated long tail of the source-reliability map, plus `source_tier`/`source_kind`/`content_kind` on `scan_items` and `intel_items`; `0049`-`0051` add no tables: the intel_metrics coverage index, the `sources.url` partial unique index after a one-time twin merge, and `intel_companies.cfpb_name`) (`0046` adds `research_prefs` — the research engine's singleton (enabled, `triage_model`, `analysis_models`) — plus `research_runs.day/lease_until/notes`, `papers.analyzed_by`, `research_step_t` values `agent`/`analyze`, and `report_kind_t` value `roundup`) (`0045` adds `home_prefs`, the Lobby widget-board singleton) (`0043` adds the **Intel Desk** six — `intel_companies` (registry in tiers; the real set seeds from the untracked `private/intel-companies.json` via `db:seed:intel`), `intel_runs` (day-keyed checkpoint + lease + notes), `intel_items`, `intel_facts` (provenance-carrying extractions, deduped on a generated `fact_key`), `intel_metrics` (LLM-free quarterly series), `intel_prefs` — see `docs/intel-desk.md`) (`0042` adds `pipeline_prefs` — the pipeline's cron/A/B singleton — plus `pipeline_runs.discovered_units`/`lease_until`, `signals.drafted_by`) (`0041` adds no tables: `scan_prefs.enrich_models text[]` + `scan_items.enriched_by` + six OpenRouter rate-card rows, the scan's cheap-provider swap) (`0040` adds no tables: `scan_runs.notes text[]`, the persisted per-run issue notes behind the /scan health panel) (`0039` adds `scan_prefs`: the scan's crons on/off singleton) (`0038` adds the **External Scan** trio — `scan_topics` (registry; the real topic set seeds from the untracked `private/scan-topics.json` via `db:seed:scan`, the migration ships a generic example), `scan_runs` (day-keyed checkpoint + lease), `scan_items` — see `docs/external-scan.md`) (`0037` adds no tables: rebuilds `signals.search_tsv` to also index `claim_touches` + `touch_details` for Ask retrieval, via the IMMUTABLE `atlas_touch_text()` wrapper because `array_to_string` is only STABLE) (`0036` adds no tables: `theses.gap_scan jsonb`, the per-thesis gap scan) (`0035` adds `company_documents`: retained browser-extracted PDF text about a Scout company, the file itself never stored) (`0033` adds `research_agent_prefs` + the agent stamp columns on `papers`; `0034` adds Startup Scout's five: `scout_verticals` (registry, query templates on the row), `companies` (library + funnel state on one row, the `papers` model; generated `name_key`, partial unique on `domain`), `company_events`, `scout_runs`, `scout_prefs` (steering + rubric singleton)) (`0032` adds the feedback box: `tickets` + `ticket_images`, images stored as bytea) (`0030` adds `generated_reports`, the Report Portal's tear sheets: insert-only, `is_published` gate; `0031` adds no tables: `search_tsv` + GIN on `papers` (partial, kept-only) and `research_threads` for /ask's research corpus) (this section narrates in detail only through `0021`; `0023` added the 7-table research subsystem, `0027` added `theses` + `thesis_reports`, and `0022`/`0024`/`0025`/`0026`/`0028` are additive column/enum changes — see the migration files. `0029` adds no tables: `search_tsv` + GIN on `sources` and `signal_candidates` for the portal's article-excerpt retrieval). Migrations `0001`–`0005` introduce the first 19: the original 14 — `questions`, `stances`, `claims`, `bridge_claims`, `node_lenses`, `edges`, `sources`, `evidence`, `positions_crosscutting`, `position_components`, `snapshots`, `rationales`, `extraction_queue`, `question_summaries` — plus `content_blocks` (`0003`), `signals` + `digest_snapshots` (`0004`), and `pipeline_runs` + `signal_candidates` (`0005`; `0005` also `alter`s `signals` to add `origin`). `0006` adds **no new tables** — it makes Evidence the canonical Signal↔Claim link by `alter`ing `evidence` (adds `signal_id`, `lens`; makes `source_id` nullable; CHECK at least one provenance) and `signals` (adds `touch_details jsonb`). `0007` also adds **no new tables** — it `alter`s `signal_candidates` to add `analysis_status` (`analysis_status_t` pending/drafted/error/discarded) + `analysis_error`, tracking analysis-step health independently of the triage funnel (additive, with a one-time backfill from `signal_id`/`triage_reason`). The additive alters continue through **`0016`**: `0015` adds `signal_candidates.source_id` + a `'source'` value to `run_cadence_t` so a manual upload runs the discovery pipeline as a pre-approved candidate; `0016` adds `signal_candidates.fetched_via` (`direct`/`jina`) + `discovery_queries` — the **learning loop** (zero-yield domains feed discovery's `blocked_domains`, triage sees per-domain track records, fetch-hostile domains route straight to the reader; see `getDomainStats`/`getZeroYieldDomains`/`isFetchHostileDomain` in `lib/data/pipeline.ts`). Later migrations also add eleven tables: `dedupe_scan` (`0010`), `reports` (`0011`), `ai_rate_cards` + `ai_cost_log` (`0014`), `0017`'s **concepts trio** (own bullet below), `concept_gap_scan` (`0018`, the persisted gap-diagnosis singleton), `0019`'s supply-chain pair (`supply_chain_node_meta` + `supply_chain_node_signals`), and `argument_gap_scan` (`0021`, the persisted argument-map gap-diagnosis singleton — the AI-proposed claims/bridges a human commits via the add-node authoring forms). RLS is **enabled with no public policies** on every table (deny-by-default; the app's DB role bypasses — all access is server-mediated).
- **`edges`** is the polymorphic argument graph: `(from_type, from_id) → (to_type, to_id)` with a `relation` (`supports`/`contradicts`/`depends_on`/`organizes`), `*_type ∈ {stance, claim, bridge_claim}`. Claims attach to **stances** and to **bridge_claims** through edges; stance↔stance "contradicts" edges encode the strongest-opposing guardrail.
- **`claims`**: a claim requires a `test` and a `domain` unless `is_frame=true` (check constraints `claims_test_required` / `claims_domain_required`). **Frames are claims with `is_frame=true`** (no domain/test/confidence) — they `organizes` other claims via edges and are quarantined from evidence.
- **`bridge_claims`** are **first-class** inter-domain objects (`domain_from`/`domain_to`, own `test`/`confidence`), not edges.
- **`evidence`** targets a `claim` or `bridge_claim` (polymorphic `target_type`/`target_id`) and carries its provenance via `source_id` (manual ingest) and/or `signal_id` (materialized from a published signal) — **both nullable, CHECK requires at least one** (`0006`). It also carries an optional `lens` (`signal_lens_t`, the audience lens the finding speaks to). `signal_id` is `on delete cascade` (a finding dies with its signal); `source_id` stays `on delete cascade`. The two writers `addEvidence`/`addEvidenceMany` are source-anchored (manual); `syncSignalEvidence` is signal-anchored (publish-time). Sourceless signal evidence (`source_id` null) shows on claim pages and feeds rollups but is excluded from the source-centric views (`getEvidenceGraph`/`getLandscape`).
- **`content_blocks`** (`0003`) — per-key text overrides for the About/landing copy (admin inline edit-mode; `atlas_edit` cookie). A missing key falls back to the code default.
- **`signals`** (`0004`) — the public Signal Board feed: a tracked development with `significance` (`significance_t` high/medium/low), `lenses` (array of `signal_lens_t`: `market`/`labor`/`geopolitics`/`regulatory`/`capability`/`society` — a **separate, audience-tailored enum** from the map's `lens_t`), `claim_touches` (a `text[]` of claim/bridge codes carried **on the row**, not a join table — resolved at read time, GIN-indexed for the reverse "signals touching this claim" query), **`touch_details jsonb`** (`0006`; `{code: {direction, reason}}` — the draft-stage detail the admin reviews, materialized into `evidence` on publish), a soft `source_id` ref, `published_at` (editorial date — drives ordering + the digest range) vs. `is_published` (visibility gate; guests see published only; **publish also materializes evidence** — see the human gate), and `origin` (`signal_origin_t` manual/pipeline, for the pipeline badge). **`digest_snapshots`** (`0004`) is the audit log for the not-yet-built digest sender.
- **Discovery pipeline** (`0005`): **`pipeline_runs`** tracks one run — `status` (`run_status_t` running/completed/failed), `step` (`run_step_t` discovery/triage/analysis/complete), `cadence` (`run_cadence_t` manual/daily/weekly/**`source`** — the last for single manual-source runs, kept out of the discovery history/analytics/lookback), running tallies, and (`0026`) `coverage jsonb` — the post-run coverage-check audit (`{since, checked_at, developments:[{headline,url,covered,matched}]}`). **`signal_candidates`** holds items to triage — `lens` (reuses `signal_lens_t`), `triage_status` (`triage_status_t` pending/approved/rejected/duplicate), `unique (run_id, url)` dedup, cascade-delete on `run_id`, a `signal_id` set when a candidate becomes a draft, and (`0015`) a `source_id` set when the candidate is a **manual upload** (so analysis reuses the curated source's text/metadata/prior instead of `ensureSource`'ing a bare row). Triage also flags any discovered URL already present in `sources` (a manual upload) or a prior draft as `duplicate`, so a manual link never re-enters discovery. These tables ARE the run's checkpoint state (so runs are resumable). Pipeline logic lives in `lib/pipeline/*`.
- **Concepts — the semantic scaffold** (`0017`): **`concepts`** (slug/name/short_definition/explanation + `status` `concept_status_t` settled/contested); **`concept_edges`** ("understand `prerequisite_id` before `concept_id`" — unique pair, no-self-loop CHECK, kept a **DAG** by the writer's in-transaction cycle walk in `lib/mutations/concepts.ts`); **`concept_claims`** (concept → claim/bridge by stable **text code**, resolved at read time like `signals.claim_touches`, dangling codes drift-flagged for admins). Both link tables carry `concept_link_status_t` (suggested/confirmed) so a future propose→queue flow needs no migration — today the app writes only `'confirmed'` (human-committed) rows and public reads filter on it. Seeded by `db:seed:concepts` (31 concepts, 51 edges; claim wiring is left to the admin). The last ten are transformer internals added for `/traceroute`, which links its 3D captions straight into these pages.
- Personal layer: `rationales` (append-only audit of confidence moves), `snapshots` (id→confidence JSON, for calibration), `positions_crosscutting` (+ `position_components`), source `reliability_prior`, `question_summaries` (per-question AI state summaries — `summary` + `metrics` JSON, newest-first).
- `snapshots` are auto-written (`post_commit`) inside `moveConfidence` on every confidence move and **read back by the `/calibration` viewer** (time-slider + trajectories + move log; `snapshotAction('manual')` is surfaced there as "Capture snapshot now").
- Still **scaffolded-but-unwired**: `extraction_queue` (we ship recommend-only, not a propose→queue→accept flow), `sources.storage_path` (PDF is text-only), and the **digest sender** (`/signals/digest` renders and `digest_snapshots` exists, but nothing writes/emails it). The `positions_crosscutting` editor is now **wired** via `/worldview`.

## Deploy (Vercel + Supabase)
- Connection precedence (`lib/db.ts`): if **`DATABASE_URL`** is set, use it; otherwise fall back to discrete **`SUPABASE_DB_*`** (direct). If neither is fully configured, `makePool()` throws. The `migrate`/`seed`/`verify` scripts always use `SUPABASE_DB_*` direct (DDL needs a session, not the transaction pooler).
- **Production MUST set `DATABASE_URL` to the Supabase POOLER (IPv4)**: `postgresql://postgres.<ref>:<pw>@aws-0-<region>.pooler.supabase.com:6543/postgres`. The direct `db.<ref>.supabase.co` host is **IPv6-only and unreachable from Vercel serverless**.
- **Pool size:** `lib/db.ts` defaults `DB_POOL_MAX` to **3** (`Number(process.env.DB_POOL_MAX || 3)`). When connecting through the transaction pooler, set `DB_POOL_MAX=1` (recommended in `.env.example`) so each serverless instance holds a single connection. **On Vercel the pooler is forced onto its transaction port** (2026-09-23): the pure rule in `lib/db-pooler.ts` (`toTransactionPoolerUrl` / `transactionPoolerPort`, tested by `scripts/test-db-pooler.mjs`) moves a `*.pooler.supabase.com` host on `:5432` or no port to `:6543`; `lib/db.ts` applies it whenever `VERCEL` is set unless `DB_POOL_MODE=session`. Session mode caps CLIENTS at the pool size (15) and every warm instance pins up to `DB_POOL_MAX` of them, so it fills after a handful of instances and further connects fail with `EMAXCONNSESSION` (admin pages 500'd while guest pages rendered). Nothing in `lib/` or `app/` relies on session state, and `withTx` keeps `BEGIN..COMMIT` on one client.
- **`AUTH_SECRET` required (≥ 32 chars)** or the app throws. Set a strong `ADMIN_PASSWORD`.
- **`ANTHROPIC_API_KEY` required for the AI features** (source dossier, PDF metadata, claim recommendations, lens recommendations, question-state summaries, media-landscape narration, Signal Board draft proposals, and the discovery pipeline's **web search**) — server-only; set it in Vercel Production and redeploy after changing it (env changes don't take effect until a redeploy).
- **Portal access**: per-person keys are issued from `/access` (no env needed beyond `AUTH_SECRET` + `APP_BASE_URL` for the magic link); **`PORTAL_KEY` (optional, legacy)** is the shared team key kept during the migration. The request form needs **`PORTAL_REQUEST_EMAIL_DOMAINS`** (comma list of work-email domains; unset = form closed with 503) and the notice email needs **`RESEND_API_KEY`** + `PORTAL_NOTIFY_TO` (falls back to `AGENT_EMAIL_TO`, then `agent_prefs.email_to`; `AGENT_EMAIL_FROM` reused). `PORTAL_KEY_DEFAULT_DAYS` (90), `PORTAL_KEY_DAILY_BUDGET_USD` (0.25) and `PORTAL_KEY_DAILY_MAX_CALLS` (60) seed each new key's limits; `PORTAL_DAILY_BUDGET_USD` (default 1.00) and `PORTAL_DAILY_MAX_CALLS` (default 200) cap the SHARED portal spend, reset at midnight UTC (raise the shared cap once more than four keys are live). `INTEL_DECK_DAILY_BUDGET_USD` (0.10) caps the company intel deck's sentence leg.
- **`CRON_SECRET` (required for the cron-driven subsystems)**: authorizes every `/api/cron/*` route (scan, pipeline, intel, research, each with `/sweep` + `/sweep2` siblings, scan/intel/research also a 15:xx `/sweep3`, plus the single-shot Friday `roundup` and the Monday `tooling` trio (`/api/cron/tooling` 07:00, `/sweep` 08:20, `/sweep2` 17:00 UTC): **twenty-four `vercel.json` entries (the hourly `/api/cron/agent` and the 12:30 UTC `/api/cron/agent/brief` joined on 2026-09-22, `/api/cron/edition` at 16:45 UTC weekdays on 2026-09-23, `/api/cron/feeds` at 20:30 UTC weekdays on 2026-09-23, `/api/cron/intel-deck` at 16:20 UTC weekdays on 2026-09-23); the sixteen engine/roundup ones weekday-daily since 2026-09-03, the tooling three Monday-only since 2026-09-17**. The third slots were Monday-only from 2026-08-31 (the 3-day lookback overran two windows); on Wednesday 2026-09-03 scan and research overran two windows on ordinary volume (2 × 700s of function time is the real daily budget, dollar caps never triggered), hence four windows for every engine; Vercel sends it as a Bearer header; the routes refuse everything when unset). `SCAN_DAILY_BUDGET_USD` (default 1.50) caps the scan's daily model spend; past the cap the run completes unenriched. `RESEARCH_DAILY_BUDGET_USD` (default 1.00) caps the research engine the same way. **`TAVILY_API_KEY`** switches the scan's search leg to Tavily's LLM-free news search (unset = the Sonnet web_search call; the account is on the **4,000-credit plan since 2026-09-23**, the free 1,000 ran out on 09-22 at ~60 queries per weekday across scan/pipeline/intel/tooling; `TAVILY_MONTHLY_CAP` defaults to 4000 for the quota tile; a Tavily 432 trips the module-state breaker in `lib/scan/tavily-breaker.ts` (`tavilyAvailable()`, re-exported from `search-tavily.ts`; 15 minutes per trip, about one work budget, since Fluid Compute reuses warm instances and the next window should re-learn with one call; only a live 432 inside `tavilyQuery`/`searchTopicNewsTavily` trips it, so callers need no `TAVILY_API_KEY` check in front of it), and the scan, intel, pipeline, and tooling engines skip their remaining search legs with ONE run note instead of firing every unit into it; `TAVILY_QUOTA_NOTE_RE` is the one matcher for both note spellings, `tavilyQuotaWarning` turns the Daily Jobs widget amber with "search legs skipped: Tavily quota" when a completed run's notes match, and the `reopen*` mutations strip those notes; `GET /api/cron/{scan,intel}?rerun=search` and `/api/cron/pipeline?rerun=discovery` reopen a completed run at its search step to recover the day once credits are back); **`OPENROUTER_API_KEY`** powers the /scan picker's cheap enrichment models (unset or nothing picked = Haiku fallback). The Tooling Monitor adds `TOOLING_WEEKLY_BUDGET_USD` (default 4), `TOOLING_PULL_BUDGET_USD` (default 12), optional `PRODUCTHUNT_TOKEN` (Product Hunt's API terms restrict commercial use; unset = source skipped), optional `GITHUB_TOKEN` (raises the search rate limit), optional `HC_PING_URL_TOOLING`.
- **Hosting is Vercel Pro since 2026-08-30** (was Hobby): up to 40 crons (each daily subsystem has its own path-keyed schedule now) and an 800s fluid-duration ceiling. The old "hard ~60s function cap" was already DEAD on Hobby (measured 2026-08-13: `maxDuration = 300` deploys and 290s streamed responses survive; the standing probe is `/api/probe/duration`, admin-gated; raw results in `docs/data-portal-upgrade-paths.md`). Most routes still declare `maxDuration = 60` and the DB-checkpointed engine decomposition remains good architecture (resumable, cheap retries); the engine cron routes declare 800 with a 700s work budget (raised from 300/270 on 2026-08-31), roundup 300.
- **Never commit `.env.local`** (`.gitignore` allows only `.env.example`).

## Gotchas
- **Do NOT run `npm run build` while `npm run dev` is running** — it clobbers `.next` and 500s the dev server.
- **URL-encode `@` in a DB password as `%40`** inside any connection string (`DATABASE_URL`), or the URL parses wrong.
- `lib/db.ts` is server-only and throws in the browser — keep it out of `'use client'` files.
- After a schema change, add a new numbered file in `supabase/migrations/` (the runner skips already-applied files; it does not diff or re-run edited ones). Number it sequentially after the highest-numbered file already in that folder.
- **Function timeouts: measured, not assumed.** The codebase was built against a believed hard ~60s Vercel cap; on 2026-08-13 the probe (`/api/probe/duration`) proved ~300s works on this Hobby project (Fluid Compute era). Existing architecture keeps `maxDuration = 60` on most routes and the discovery pipeline stays **decomposed into many short, DB-checkpointed steps** (one lens batch, ≤3 searches ≈ 45s) because that buys resumability and cheap retries, not because the platform demands it. When a single longer function is genuinely simpler, declare `maxDuration` up to ~300 and keep the SDK timeout below it. `runStructured` (`lib/dossier.ts`) stays single and non-web for latency and cost reasons, not the old cap.
- **React Compiler lint rules are errors, with no opt-out** (`react-hooks/refs`, `set-state-in-effect`, `immutability`, `purity`, from `eslint-plugin-react-hooks@7` via `eslint-config-next`). Practical consequences: never read `ref.current` during render (resolve it in the handler or an effect), never `setState` synchronously in an effect body (use `useSyncExternalStore` or a callback), and keep mutation of external objects (a three.js scene) in module-scope functions called from effects. The repo's bar is a zero-warning run.
- **`three` geometry merging:** `mergeGeometries` refuses to mix indexed and non-indexed inputs, and `RoundedBoxGeometry` is non-indexed while `BoxGeometry`/`CylinderGeometry`/`TorusGeometry` are indexed. Normalise with `.toNonIndexed()` first. This failed silently once and dropped every detail feature on 20 objects. Also note core `LineDashedMaterial` has no `dashOffset` in 0.180.
- **`proxy.ts`'s matcher does not exclude `/api`.** A new public API route needs its own allow-list entry or it 307s to `/login`.
- **`signal_lens_t` ≠ `lens_t`.** The Signal Board's six audience lenses are a separate enum from the argument map's seven `lens_t` lenses — don't conflate them.
- **The pinned Anthropic SDK (0.100.1) stream accumulator predates server-tool blocks**: on
  a `messages.stream` call with `web_search`, `finalMessage().content` collapses to bare
  text blocks (no `server_tool_use`/`web_search_tool_result`, no citations). Capture web
  sources from the RAW `streamEvent`s instead (`content_block_start` of type
  `web_search_tool_result`, plus `citations_delta` when the model quotes) — the ask routes
  do exactly this. Re-check on any SDK upgrade.
- **`ai_cost_log.pipeline_run_id` is FK'd to `pipeline_runs`** — and `recordApiCall` swallows every insert error. A non-pipeline feature that passes its own run id there loses the cost row SILENTLY (and any budget guard summing that feature goes blind). Scan/scout calls carry provenance in `metadata` (`scan_run`/`scout_run`) and never set `pipelineRunId`.
- **`claude-haiku-4-5` rejects the `effort` parameter** (live-verified 2026-08-28: 400 "This model does not support the effort parameter"). `runStructured` therefore omits `thinking` + `output_config` whenever `opts.model` is set; keep that behavior if you touch it.
- **`enforceCitations` counts a surviving link as cited ONLY through `tagByHref`.** An allow-list with a populated `hrefs` set but an empty map gates correctly and still reports `cited.length === 0`, so a caller that drops "sentences with no surviving link" drops everything (the first live intel deck lost all 24 sentences this way). When there are no S/P tags, map every href to itself.
- **react-pdf: the registered font families are `Anton`, `Schibsted`, `JetBrains`** (`lib/pdf/shell.tsx`), not the Google names; an unregistered family 500s the whole PDF. And a `flex: 1` child inside the content-sized page body measures 0 tall (every row paints on one line, the page spills onto a second one); 16:9 slides need fixed heights + `overflow: 'hidden'`.
- **The legacy shared-key cookie is fingerprinted** (`portal:legacy:<hmac(legacy:PORTAL_KEY)>`, `legacyCookieGrants` in `lib/portal/keys.ts`): unsetting `PORTAL_KEY` logs every legacy holder out, rotating it logs out everyone minted after 2026-09-24 (bare pre-change cookies age out within 30 days). Legacy-key guesses are throttled per hashed IP (`lib/rate-limit.ts`, 10 failures / 10 min, separate `form` and `hdr` buckets) and a limited client gets the same "That key did not work." as a wrong key; per-person `atlas_` keys never touch the limiter. Never add a public API route without either a gate marker (`isAdmin(` / `cronGate(` / `identityFromRequest(` / `getPortalIdentity(` / `isPortal(`) or an entry in `scripts/test-api-gates.mjs`'s EXEMPT set with a reason.
- **The model's per-touch reason is admin-only everywhere.** `syncSignalEvidence` copies it into `evidence.excerpt` on publish, so `getEvidenceFor` and `buildEvidenceLedger` null the excerpt on signal-anchored rows for guests; only the key-gated `signals-export` ships it. Source-anchored excerpts (quoted passages) stay public.
- **No em dashes in user-facing text.** Rendered UI strings use a comma/colon/period, ` · ` for label separators, and `–` (en dash) for null placeholders — never `—`. Every user-visible AI prompt (`dossier.ts`, `summary.ts`, `concepts.ts`, `report-generate.ts`, `tearsheet/generate.ts`, `pipeline/{analysis,triage,dedupe}.ts`) carries a "never use an em dash" instruction; keep it when editing prompts. (Code comments are exempt.)
