// The tooling monitor's third weekly invocation (evening resume for a
// weekly run that overran the morning windows). Same handler, same gate.
export { GET } from '../route';
export const dynamic = 'force-dynamic';
export const maxDuration = 800;
